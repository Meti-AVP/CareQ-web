# سجل الملاحظات المركزي — carq-web

السجل الوحيد اللي بيتراكم عبر كل المراحل. كل ملاحظة ليها ID ثابت من أول ما
تظهر لغاية ما تتقفل. اتعمل أول مرة في المرحلة ٠ (قراءة كود فقط — مفيش إصلاح).

الأعمدة: `ID` | `الخطورة` (P0-P3، تعريفها في `MISSION.md §4.7`) | `الملف:السطر` |
`القاعدة` (كود المواصفة لو موجود) | `الوصف` | `الحالة`.

---

| ID | الخطورة | الملف:السطر | القاعدة | الوصف | الحالة |
|---|---|---|---|---|---|
| FND-001 | P0 | `apps/admin/src/app/(dash)/layout.tsx:1-6`، `apps/dealers/src/app/(portal)/layout.tsx:1-6` | I-4، §2 (ADMIN) | مفيش أي فحص جلسة/دور على مستوى الـlayout ولا middleware ولا أي hook — أي زائر بيفتح أي مسار محمي (`/listings`, `/auctions/[id]` إلخ) مباشرة بره الـ`/login` بيشوف اللوحة كاملة بلا أي بوابة. اتأكد بقراءة الكود مباشرة + بحث شامل عن `middleware.ts` (صفر نتائج) وعن أي `useSession`/`redirect(`/`requireAuth` (صفر نتائج) في التطبيقين. | **اتصلّح في المرحلة ٢** — `middleware.ts` (حارس edge) + فحص كوكي سيرفر-سايد تاني في `layout.tsx` (دفاع مزدوج)، مؤكّد بـe2e (`وصول مباشر لمسار محمي من غير جلسة بيرجّع لـ/login`) |
| FND-002 | P1 | `apps/admin/src/app/login/page.tsx:86,106-110`، `apps/dealers/src/app/login/page.tsx` (`requestCode`/`verify`) | §2 (ADMIN)، PORTAL §8.1 | فلو الدخول في التطبيقين وهمي بالكامل: `requestOtp`/`verifyOtp` (أدمن) و`requestCode`/`verify` (معارض) بيعملوا `setTimeout` بدل نداء `POST /v1/auth/otp/*` الحقيقي. الأدمن بيحط توكن حرفي `'demo-access-token'` (`login/page.tsx:110`)، والمعارض بتقبل أي ٦ أرقام. الشاشتين فيهم بانر صريح بيقول ده وضع ديمو. | **اتصلّح في المرحلة ٢** — `requestOtp`/`verifyOtp` في `packages/api-client/src/auth.ts` بتوصّل فعليًا لـ`POST /v1/auth/otp/*` لما `NEXT_PUBLIC_API_URL` يتحط؛ وضع الموك بقى محكوم بـ`DEMO_MODE` صريح (مش افتراضي صامت) وبيتقفل تلقائيًا مع الباك اند |
| FND-003 | P1 | كل التطبيقين — `Shell.tsx` (أدمن وdealers) | §2.8 (ADMIN) | مفيش زرار خروج (logout) في أي من الـShell بتوع التطبيقين — بحث شامل عن logout/خروج في الكود رجّع صفر عناصر تفاعلية بهذا الغرض. | **اتصلّح في المرحلة ٢** — زرار خروج في `Shell.tsx` (اللوحتين) بينادي `useSession().logout()`، مؤكّد بـe2e |
| FND-004 | P2 | `packages/api-client/src/client.ts:55` | I-4، §2 (ADMIN) | `tokenStore.isExpiring()` معرّفة لكن **محدش بينادها في المشروع كله** (تأكيد بـgrep) — يعني النظام بيستنى `401` بدل ما يجدّد استباقيًا قبل الانتهاء. | **اتصلّح في المرحلة ٢** — `session-context.tsx` دلوقتي بيراقب كل ٢٠ ثانية وبينادي `refreshSession()` لو `isExpiring()` رجّعت true، قبل ما أي نداء ياخد 401 |
| FND-005 | P1 | `packages/api-client/src/client.ts` (دالة `http`) | §3.6 عام (المرجع في MISSION.md) | `http()` مفيهاش `AbortController` ولا أي timeout — تأكيد بـgrep شامل عن `AbortController`/`signal` في `packages/` و`apps/` (صفر نتائج بره التعريف نفسه). طلب معلّق = spinner للأبد من غير رسالة. | **اتصلّح في المرحلة ٢** — `http()` دلوقتي فيها `AbortController` + `timeoutMs` (افتراضي ٢٠ ثانية، `REPORT_TIMEOUT_MS`=٤٥ للتقارير)، ومدموجة مع أي `signal` بيبعته المنادي |
| FND-006 | P1 | `packages/api-client/src/admin/hooks.ts:194`؛ `packages/api-client/src/dealers/hooks.ts:123,297,315,380,399,419,466` | X-3 | `idempotencyKey()` (`client.ts:46-48`) بترجّع مفتاح جديد كل نداء (`${prefix}-${Date.now()}-${random}`) وبتتنادى **جوه** الـ`mutationFn` في كل الأماكن دي — يعني إعادة محاولة/دوسة تانية سريعة بتولّد مفتاح مختلف فتتنفّذ العملية مرتين. أخطر ٣ حالات: `usePlaceBid` (مزايدة بفلوس)، `useCreateEntry` (دخول مزاد)، `useSendLeadMessage`. الاستثناء الصح الوحيد: `useBulkCreate` (مفتاح ثابت `bulk-${uploadId}-${index}`). | **اتصلّح في المرحلة ٥** — `IdempotencyKeyCache` (`client.ts`) بترجّع نفس المفتاح طول ما العملية المنطقية لسه معلّقة وتتصفّر بعد النجاح؛ الـ٩ مواضع كلها بقت بتستخدمها (`useCreateListing`, `usePlaceBid`, `useCreateEntry`, `useMarkListingSold`, `useReactivateListing`, `useRenewListing`, `useUploadListingPhoto`, `useSendLeadMessage`, `useOfferSellNow`)، مؤكّد باختبارات وحدة (`idempotency.test.ts`) |
| FND-007 | P2 | `packages/api-client/src/admin/hooks.ts:339-345` (`useExhibitionBids`) | — | تعليق صريح في الكود: `GET /v1/admin/exhibitions/{id}/bids` مش موجود في الباك، فالبديل الحالي (موك) نداء لكل مزاد لوحده = N+1 لو الـendpoint اتبنى بنفس النمط. | مفتوح — مؤجّل لمرحلة ٥ (توثيق في `docs/BACKEND-CONTRACT.md`) |
| FND-008 | P2 | `packages/api-client/src/admin/hooks.ts:428,438` (`useAuction`, `useAuctionBids`) | — | الـhooks دول جوه `admin/hooks.ts` بينادوا على `/v1/auctions/{id}` و`/v1/auctions/{id}/bids` (مسار عام، مش `/v1/admin/...`) — لازم مراجعة في مرحلة ٥ هل ده مقصود (نفس مسار المعارض) ولا لازم نسخة أدمن منفصلة بصلاحيات مختلفة. | **اتحل في المرحلة ٥ — مقصود، مش باج.** راجعت `docs/ADMIN_DASHBOARD_SPEC.md §6.1/§6.2` كاملين: فيه `GET /v1/admin/auctions?status=&cursor=` (قايمة) بس **مفيش** `/v1/admin/auctions/{id}` ولا `/{id}/bids` معرّفين خالص — الـendpoint التفصيلي الوحيد الموثّق في أي من المواصفتين هو العام (`PORTAL §8.1`)، ومتّسق مع `A-12` (اسم المزايد علني داخل المزاد بالتصميم، مفيش داعي لنسخة أدمن مقيّدة). موثّق في `docs/BACKEND-CONTRACT.md` كتأكيد للباك مش كطلب endpoint جديد |
| FND-009 | P2 | `docs/ADMIN_DASHBOARD_SPEC.md §3` (خريطة الشاشات، سطر ١٠٠-١١٦) مقابل `§5` (كتالوج التشارتس، سطر ٤٥١-٤٦٠) | C-40..C-45 | كتالوج التشارتس بيعرّف قسم «النشاط والثقة» (`C-40` رسايل/يوم … `C-45` أشهر البحثات) لكن خريطة الشاشات في نفس المواصفة **مالهاش مسار `/activity` أو مقابل** — فمفيش شاشة تستضيف الرسوم دي أصلًا، مش بس الرسوم ناقصة. اتأكد بجرد كامل لمسارات `apps/admin/src/app` (١٦ مسار، ولا واحد فيهم بيتطابق) وجرد `packages/ui/src/charts` استخدامات. | مفتوح — **سؤال محتاج قرار، اتسجّل في PHASE-0-RECON.md قسم الأسئلة** |
| FND-010 | P3 | `packages/ui/src/components/Primitives.tsx:229` (`Shine`) | — | مكوّن `Shine` مُصدَّر من `index.ts:24` لكن **مالوش أي استخدام** — لا في `apps/` ولا داخليًا في أي مكوّن تاني بـ`packages/ui`. كود ميت. | مفتوح — مؤجّل لمرحلة ٨ (نظافة الكود) |
| FND-011 | P3 | `packages/ui/src/styles.css:11-27,90` | §3.1 قاعدة ٣ (المشروع) | القاعدة بتقول «ممنوع hex خارج `tokens.ts` و`charts/theme.ts`» — لكن `styles.css` (مش أي من الملفين) فيه ١٧ قيمة hex (نسخة يدوية من `tokens.ts` كمتغيرات CSS، لازمة تقنيًا لأن CSS مايقدرش يستورد TS) + سطر واحد مستقل (`:90 color:#fff`) مش مربوط بأي توكن. `scripts/static-checks.mjs` **مابيفحصش الملف ده** لأنه بيمشي بس على `.ts`/`.tsx` وبيفحص الـhex بس لو الملف تحت `apps/*/src` — الفحص التلقائي عنده ثغرة تغطية هنا. | مفتوح — مؤجّل لمرحلة ٨ (أو تحديث `static-checks.mjs` ليغطي `.css`) |
| FND-012 | P3 | `scripts/static-checks.mjs` (كل الملف) | §3.1 قاعدة ٩ | السكريبت بيفرض ١٠ من ١٢ قاعدة في `MISSION.md §3.1` (مفحوص بقراءة الملف كامل) — **قاعدة ٩ (ممنوع `row-reverse` يدوي) مش متفروضة أصلًا** بالسكريبت. تحقق يدوي (`grep -rn "row-reverse\|flexDirection"`) لقى القاعدة **متبّعة فعليًا** حاليًا (مفيش استخدام حقيقي، بس تعليقات بتوضّح القرار) — يعني مفيش مخالفة النهاردة، بس الفحص الآلي مش بيحميها من الانحراف مستقبلًا. | مفتوح — مؤجّل لمرحلة ٨ |
| FND-013 | P3 | `apps/admin/src/app/(dash)/financing/page.tsx` و`financing/[id]/page.tsx` (`PARTNER_LABELS`) | — | قاموس `PARTNER_LABELS = {jameel:'جميل'}` (شريك تمويل واحد) مكرّر حرفيًا في ملفين بدل مصدر واحد مشترك — تكرار منطق بسيط (`MISSION.md §3.5.5`). | مفتوح — مؤجّل لمرحلة ٨ |
| FND-014 | P3 | `docs/ADMIN_DASHBOARD_SPEC.md:25`، `docs/EXHIBITION_PORTAL_SPEC.md:179` | D-2, D-4 | كودي القاعدة `D-2` و`D-4` بيتحالوا على `docs/BUSINESS_RULES.md` كمرجع — الملف ده **مش موجود في هذا الريبو** (اتأكد: `docs/` فيها بس المواصفتين). المعنى العملي لـ`D-2` واضح من السياق (أول صورة بتفعّل الإعلان) ومطبّق فعلًا في الكود، لكن `D-4` (قرار بشري لطلبات بيع حالًا فوق السقف) مذكور مرة واحدة بس بلا تفصيل إضافي. | مفتوح — فجوة توثيق (مش كود) — ملاحظة لـ`docs/BACKEND-CONTRACT.md` في المرحلة ٥/٩ |
| FND-015 | P2 | `npm audit` (خط الأساس) | — | كانت ٧ ثغرات كلها في `devDependencies` غير مباشرة: `esbuild`/`vite`/`vite-node`/`@vitest/mocker` (عبر `vitest`) و`postcss` (عبر نسخة `next` الداخلية القديمة). كلها أدوات تطوير/بناء، مش تبعيات إنتاج شغّالة في المتصفح وقت التشغيل. | **جزء اتصلّح فعليًا (`FND-063`)، والباقي قرارها اتاخد صراحة يتساب.** شق الـ`postcss` (الأخطر — ٤ ثغرات "عالية": XSS + قراءة ملفات) اتصلّح بأمان صفري المخاطر من غير أي ترقية breaking — تفاصيل `FND-063`. الباقي (`esbuild`/`vite`/`@vitest/mocker` عبر `vitest`) مالوش إصلاح آمن — الوحيد المتاح قفزة **مجورين رئيسيين** (`vitest` مثبّتة فعليًا `2.1.9`، والإصلاح يطلب `4.1.11` — مش `3.x` وسيطة حتى) بتحتاج إعادة تحقق كامل لسويت الاختبارات الـ١٥١. **القرار: تتساب مفتوحة عمدًا** — أدوات اختبار بس، مخاطرة الترقية (كسر اختبارات شغّالة) أكبر من فايدتها الأمنية الفعلية (سيرفر تطوير محلي/موك اختبار، مش كود إنتاج) لحد ما يبقى فيه باك اند حقيقي متوصّل ووقت مخصّص للترقية والتحقق الكامل |
| FND-016 | P1 | `packages/api-client/src/server/session.ts` (كان `SESSION_COOKIE = 'cq_session'` ثابت) | I-4 | **باج اتلقط واتصلّح في المرحلة ٢.** كوكي الجلسة كانت باسم واحد ثابت للتطبيقين. كوكيز المتصفح بتتحدد بالـhost بس مش بالـport — `localhost:3100` (أدمن) و`localhost:3200` (معارض) بيتشاركوا نفس الـhost محليًا، فكوكي أدمن كانت بتتقرا كصالحة في بوابة المعارض (والعكس) على نفس الجهاز. اتلقطت بالدليل: `e2e/global-setup.ts` كان بيفشل — تسجيل دخول أدمن ثم فتح `/login` بتاع المعارض في نفس المتصفح كان بيرمي لـ`/` بدل فورم الدخول (الميدلوير شايف كوكي "صالحة" مش بتاعته). في الإنتاج المشكلة مش موجودة (دومينات مختلفة تمامًا: `admin.carq.eg`/`dealers.carq.eg`)، بس كانت هتكسر أي تطوير/اختبار محلي بصمت. | **اتصلّح** — اسم كوكي مختلف لكل تطبيق (`cq_session_admin`/`cq_session_dealers`) عن طريق `createSessionHandlers(cookieName)` + `apps/{admin,dealers}/src/lib/session.ts`، موثّق في `docs/AUTH.md §الباج اتصلّح أثناء المرحلة` |
| FND-017 | **P0** | `apps/dealers/src/components/Providers.tsx` (كان `SessionProvider` فيها) | PORTAL §2، `MISSION.md` بند ٣ مرحلة ٢ | **باج اتلقط واتصلّح في المرحلة ٢ — فيتشر أساسي كان معطّل بالكامل.** `/apply` و`/apply/status` (المسار العام الوحيد لفرد عادي يقدّم كطلب معرض، مسار مقصود صراحة حسب `PORTAL §2`) كانوا بيترحّلوا تلقائيًا لـ`/login` رغم إن `middleware.ts` كان مستثنيهم صح. السبب: `SessionProvider` (وجوّاه effect بيعمل `router.replace('/login')` لو `status === 'unauthenticated'`) كان متحط في الـroot layout اللي بيغلف التطبيق كله، مش بس المسارات المحمية. اتلقط بالدليل: فتح `/apply` من غير أي كوكي بمتصفح حقيقي بيعمل نداء `POST /api/session/refresh` (بيرجع 401 لعدم وجود كوكي) وبعدها `page.url()` بيتحول فعليًا لـ`http://localhost:3200/login` (تسجيل شبكة كامل). **النتيجة العملية: مفيش حد كان يقدر يقدّم كطلب معرض جديد أصلًا.** | **اتصلّح** — `SessionProvider` اتنقل من `Providers.tsx` العام إلى جوّه `(dash)/layout.tsx` و`(portal)/layout.tsx` بس (حوالين `<Shell>`، في التطبيقين للاتساق)، مؤكّد بفتح `/apply` فعليًا (فضلت زي ما هي) وبـe2e (`e2e/dealers.spec.ts:193`) |
| FND-018 | P1 | `packages/api-client/src/admin/hooks.ts` (`useMarkEntryPaid`, `useMarkDefaulted`) | `ADMIN §10` بند ٥ (سبب مكتوب إجباري) | **باج اتلقط واتصلّح في المرحلة ٣.** الاتنين من ضمن الأكشنات الخطرة (تأكيد دفع دخول مزاد، تعليم مزاد متعثر) — الـ`type` كان بيطلب `reason` إجباري، والموك كان بيستخدمه ويسجّله في `audit_log`، **بس نداء الـhttp الحقيقي (`POST /v1/admin/auction-entries/{id}/paid` و`POST /v1/admin/auctions/{id}/default`) ماكانش بيبعت أي `body` خالص.** يعني السبب اللي الأدمن بيكتبه في الـdialog مش هيوصل للسيرفر الحقيقي أبدًا لما الباك اند يتوصّل — مخالفة صريحة لبند ٥ "تأكيد مزدوج + سبب مكتوب إجباري". | **اتصلّح** — الاتنين بقوا بيبعتوا `body: { reason }` |
| FND-019 | P2 | `packages/api-client/src/admin/hooks.ts` (الأكشنات الخطرة السبعة) | `MISSION.md §3.6` بند ٤ (idempotency للأكشنات الخطرة، مرتبط بـ`X-3`) | **مفيش `Idempotency-Key` على أي من الأكشنات الخطرة السبعة** (منح التعاقد، مراجعة طلب، ترقية دور، إيقاف، تعليم متعثر، تأكيد دفع، شارات الثقة) — نفس نمط `FND-006` (idempotency في `dealers/hooks.ts`، مؤجّل لمرحلة ٥)، بس هنا في أكشنات إدارية حساسة (خصوصًا التعاقد وترقية الدور). دوسة مزدوجة سريعة أو إعادة محاولة بعد timeout ممكن تنفّذ الأكشن مرتين. | **اتصلّح في المرحلة ٥** — نفس `IdempotencyKeyCache` (`FND-006`) اتطبّق على كل الأكشنات العشرة اللي كانت بلا idempotency في `admin/hooks.ts` (السبعة + `useCollectSellNow`, `useSetListingStatus`, `useRevealPhone`) — كل واحدة بمفتاح تمييز (`disambiguator`) يعكس شكل العملية (مثلًا `${id}:${status}` أو `${id}:${role}`) |
| FND-020 | P3 | `apps/admin/src/app/(dash)/exhibitions/[id]/page.tsx` (`ConfirmDialog` بتاع `useSetContract`) | `ADMIN §10` بند ٥، `ADMIN §11` | كان بس ترقية الدور وتعليم مزاد متعثر بيطلبوا `typeToConfirm` (كتابة كلمة تأكيد إضافية) في الـdialog. التعاقد (بيفتح فلوس حقيقية) والإيقاف وشارات الثقة كانوا بيكتفوا بسبب + تأكيد الـdialog العادي. | **اتحل بقرار من المستخدم** — منح التعاقد بقى بيطلب `typeToConfirm="تعاقد"` برضه (نفس مستوى الترقية/التعثر). إيقاف التعاقد والإيقاف وشارات الثقة فضلوا من غير `typeToConfirm` عن قصد (أقل خطورة ماليًا) |
| FND-021 | **P1** | `apps/dealers/src/app/(portal)/leads/page.tsx:154,427` | `ADMIN §10` بند ٢، `MISSION.md` مرحلة ٤ بند ٢ | **باج اتلقط واتصلّح في المرحلة ٤.** صفحة "المهتمين" في بوابة المعارض كانت بتعرض تليفون المستفسر (المشتري المحتمل) **كامل غير مخفي** (`formatPhone` بدل `maskPhone`) في عمود الجدول وفي subtitle تفاصيل المحادثة — مخالفة مباشرة لقاعدة "التليفونات مخفية جزئيًا افتراضيًا في كل جدول وكارت". غير متسق تمامًا مع `useRevealPhone` المطبّقة بدقة في لوحة الأدمن (لكن غير موجودة في `dealers/hooks.ts` خالص). | **اتصلّح** — الاتنين بقوا بيستخدموا `maskPhone`. التصدير (CSV) كان أصلًا آمن (العمود بيصدّر الاسم بس مش التليفون) |
| FND-022 | P2 | `packages/api-client/src/admin/hooks.ts` (`useOfferSellNow`, `useCollectSellNow`, `useSetFinancingStatus`, `useRevealPhone`) | `ADMIN §10` بند ٤ | الأربعة دول كانوا بيسجّلوا صف تدقيق فعليًا في الموك (`audit(...)`)، بس **مبيعملوش `invalidateQueries(['admin','audit'])`** بعد النجاح — يعني شاشة `/audit` مش هتعكس الصف الجديد لايف لو مفتوحة في تبويب تاني. `useSetFinancingStatus` بالذات خطيرة لأن الواجهة بتوعد المستخدم نصًا "بيتسجّل في سجل التدقيق". | **اتصلّح** — الأربعة بقوا بيعملوا invalidate. `useRevealPhone` كمان ماكانش عندها `useQueryClient()` أصلًا |
| FND-023 | P2 | `packages/api-client/src/mock/db.ts::setFinancingStatus` | F-6 | الواجهة (`financing/[id]/page.tsx`) بتوعد صراحة إن رفض طلب تمويل بيمسح صور البطاقة من التخزين بعد القفل، بس دالة الموك **ماكانتش بتحدد `idImagesDeletedAt` عند الرفض خالص** — القيمة الوحيدة الظاهرة كانت مصدرها الصدفة العشوائية في بيانات الـseed مش فعل الرفض الحقيقي. | **اتصلّح** — الرفض بيحدد `idImagesDeletedAt = now()` فعليًا |
| FND-024 | P2 | مفيش `@media print` في المشروع كله | F-6 | صور البطاقة (وأي `dialog` حساس) كانت بتتطبع عادي تمامًا لو المستخدم عمل Ctrl+P — باب خلفي فعلي لـ"تنزيل" بيانات شخصية رغم القاعدة الصريحة "متكاشيش الصورة ولا تنزّلها". | **اتصلّح** — قاعدة CSS عامة في `packages/ui/src/styles.css` بتخفي أي `[role="dialog"]` وقت الطباعة، مؤكّدة بـe2e |
| FND-025 | P2 | `packages/api-client/src/server/session.ts` (route handlers الجلسة) | عام (دفاع CSRF) | مفيش أي فحص لهيدر `Origin`/`Referer` في أي route handler — الحماية من CSRF كانت معتمدة كليًا على `SameSite=Lax` (دفاع قوي لكنه مش كامل ١٠٠٪). | **اتصلّح** — `isTrustedOrigin()` بترفض أي طلب لـ`/api/session/*` لو `Origin` موجود ومختلف عن أصل الطلب، بـ`403`. مؤكّد بـe2e إن الرفض شغال وإن فلو الدخول الطبيعي لسه سليم |
| FND-026 | P3 | `packages/api-client/src/admin/hooks.ts::useSignedIdImage` (تعليق) | توثيق | تعليق بيدّعي `gcTime: 0` غير موجود فعليًا في الكود — وأصلًا `gcTime` مش خيار صالح لـ`useMutation` (خاص بـ`useQuery` بس). الحماية الفعلية (رابط في `state` مؤقت بيتمسح عند القفل) موجودة في الصفحة نفسها. | **اتصلّح** — التعليق بقى يعكس الواقع بدقة |
| FND-027 | P2 | `apps/admin/src/app/(dash)/exhibitions/requests/page.tsx` | F-6 | أوراق طلبات ترقية المعارض (سجل تجاري/بطاقة ضريبية) مش قابلة للعرض — الـendpoint الموقّع (`GET /v1/admin/exhibitions/applications/{id}/document`) ناقص في الباك. **قرار آمن ومقصود** (الصفحة بتعرض تنويه واضح بدل ما تبني رابط عام بإيدها)، موثّق بالفعل بشكل ممتاز في الكود نفسه. | مفتوح — طلب توثيق لـ`docs/BACKEND-CONTRACT.md` في المرحلة ٥ |
| FND-028 | P3 | `packages/api-client/src/dealers/hooks.ts::useDeleteListing` | X-6 (مفيش حذف) | في وضع الموك: soft delete مؤكّد (`status='removed'`). في وضع الباك الحقيقي: نداء HTTP `DELETE` — السلوك الفعلي (soft أو hard) بيتحدد بالباك اند نفسه، مش دليل قاطع من كود الفرونت. | مفتوح — طلب تأكيد صريح من فريق الباك في المرحلة ٥ |
| FND-029 | P1 | `apps/admin/src/app/(dash)/listings/page.tsx` | `ADMIN §4.3` (قائمة الأعمدة) | عمود "تاريخ الانتهاء" (`expiresAt`) مذكور صراحة في قائمة أعمدة `/listings` بالمواصفة، وكان غائب تمامًا من `columns` رغم إن `Listing.expiresAt` متاحة ومستخدمة بالفعل في `/listings/[id]`. | **اتصلّح في المرحلة ١** — عمود جديد بنفس نمط عمود "تاريخ النشر" (`hideBelow:'xl'`)، مؤكّد بفتح الصفحة فعليًا (الجدول عريض ومحتاج scroll أفقي ليظهر، سلوك طبيعي) |
| FND-030 | P1 | `apps/admin/src/app/(dash)/exhibitions/page.tsx` (`ConfirmDialog` بتاع `useSetContract` من شاشة القائمة) | `ADMIN §10` بند ٥ (امتداد لـ`FND-020`) | منح التعاقد من شاشة `/exhibitions` (القائمة) كان بيكتفي بسبب + تأكيد `dialog` عادي، بينما نفس الأكشن بالظبط من `/exhibitions/[id]` بيطلب `typeToConfirm="تعاقد"` — نفس الأكشن الخطر بمستويين تشدد مختلفين حسب نقطة الدخول. | **اتصلّح** — `typeToConfirm="تعاقد"` عند المنح في شاشة القائمة كمان، مؤكّد فعليًا بفتح `Switch` معرض غير متعاقد والتأكد من ظهور خانة الكتابة |
| FND-031 | P1 | `packages/api-client/src/dealers/hooks.ts::useMyEntries` | `PORTAL §10` بند ٧ (`staleTime: 0` على كل ما يخص المزايدة) | `entry.paidAt` جزء مباشر من الشرط الثالث للمزايدة (A-1)، لكن `useMyEntries` (بعكس `useDealerAuction`/`useDealerBids` المشابهين) ماكانتش عندها `staleTime: 0` ولا `refetchInterval` — لو المزايد قاعد في الغرفة مستني تأكيد دفع الأدمن، مفيش أي تحديث تلقائي لحالته غير لو رجّع focus للنافذة. | **اتصلّح** — أضيفت `staleTime: 0` و`refetchInterval: 10_000` بنفس نمط أخواتها |
| FND-032 | P2 | `packages/ui/src/charts/theme.ts::divergingColor` + `docs/ADMIN_DASHBOARD_SPEC.md` (كتالوج C-13) | `ADMIN §5` | نص كتالوج C-13 بيقول حرفيًا "تيل = فوق الاقتراح، برتقالي = تحته"، لكن الكود الفعلي معكوس (تيل=تحت، برتقالي=فوق) — ومتّسق مع استخدام مشابه لنفس المقياس في `listings/[id]` (فرق السعر عن `market_avg`). يبدو قرار تصميم متعمّد من فريق سابق يخالف النص الحرفي، مش سهو عشوائي. | **اتصلّح — القرار: نص المواصفة هو الغلط** — الكود متسق مع `listings/[id]` وعكسه هيكسر شاشتين مش واحدة؛ `docs/ADMIN_DASHBOARD_SPEC.md` صُحّح ليطابق السلوك الفعلي (مع توضيح سبب الاتساق) |
| FND-033 | P2 | كتالوج التشارتس — قسم "النشاط والثقة" بالكامل | `ADMIN §5` (C-40 حتى C-45) | ٦ رسوم بيانية (رسايل/يوم، محادثات جديدة/يوم، إشعارات بالنوع، تغطية الثقة، تغطية التسعير على الوقت، أشهر البحثات) موجودة في كتالوج التشارتس **بدون أي صفحة/route تستضيفهم** — لا `/chat` ولا `/activity` ولا `/trust` في خريطة الشاشات أصلًا (نفس ملاحظة `FND-009` من المرحلة ٠، مؤكَّدة الآن بتفصيل كامل). يبدو غياب متعمّد من نطاق البناء الحالي مش سهو، لكنه فجوة كتالوج حقيقية. | مفتوح — **القرار: يتأجّل عمدًا، يفضل في الكتالوج** — الرسوم دي بتوصف شاشات مستقبلية كاملة (محادثات، ثقة) مش تعديل صغير على شاشة موجودة؛ شطبها من الكتالوج هيضيّع مواصفة موجودة لفيتشر لسه ملموش بناء، وبناؤها فعليًا أكبر من نطاق مرحلة المطابقة. تفضل مرصودة لمرحلة/تكليف منفصل |
| FND-034 | **P0** | `apps/dealers/src/app/(portal)/inventory/bulk/page.tsx` | `PORTAL §4.3` (خطوة ٣ من ٤: ربط الصور) | **أخطر فجوة اتلقطت في المرحلة دي.** المواصفة بتصف صراحة خطوة ربط الصور بالصفوف (مجلد مضغوط أسماء ملفاته = رقم الصف، أو سحب وإفلات لكل صف) كجزء أساسي من فلو الرفع بالجملة. الكود الفعلي عنده ٤ خطوات بس: قالب → رفع CSV → مراجعة → إرسال — **مفيش أي خطوة أو مكوّن لربط الصور خالص**. النتيجة العملية: كل عربية تتولد بالجملة بتبقى `draft` بلا صور، وتفضل مخفية لحد ما المعرض يرفع صورها **يدويًا واحدة واحدة** من `/inventory` — بالظبط الشيء اللي القسم كله (رفع ٤٠+ عربية دفعة واحدة) اتصمم عشان يتجنّبه. | **اتصلّح** — خطوة "صور العربيات" جديدة في `/inventory/bulk`: اختيار مجلد كامل (`FileDrop` بخاصية `directory`/`webkitdirectory`، بديل بدون مكتبة فك ضغط لـ"مجلد مضغوط") بيوزّع كل صورة على صفها تلقائي حسب رقم بادئ اسم الملف (`1.jpg`→صف ١)، أو رفع/شيل لصف واحد من عمود "الصور" الجديد في جدول المراجعة. عند الإرسال، `send()` بترفع صور كل صف بعد نجاح إنشاؤه (`useUploadListingPhoto` بالتوالي، فشل صورة واحدة مايوقفش باقي الصف). اكتُشف كمان أثناء البناء إن `useBulkCreate`/`useCreateListing` في وضع الموك ماكانوش بيسجّلوا الإعلان في `db.listings` خالص (نجاح وهمي بلا أثر) — أُصلح بإضافة `mock.createListing()` واستخدامها في `useBulkCreate` (فيها `listingId` حقيقي دلوقتي يتمرر لخطوة رفع الصور) |
| FND-035 | P2 | `apps/dealers/src/lib/catalog.ts::isEgyptianPhone` مقابل `apps/dealers/src/app/apply/page.tsx` (النص) | `PORTAL §2.2` | نص فورم طلب الترقية بيقول صراحة "موبايل مصري (010/011/012/015) **أو أرضي**"، لكن `isEgyptianPhone()` بتفحص نمط الموبايل بس (`^01[0125]\d{8}$}`) وبترفض أي رقم أرضي فعليًا رغم وعد النص. | **اتصلّح — القرار: نصحح النص ليطابق الواقع (موبايل بس)** — تخمين نمط أرضي مصري بدون تأكيد من فريق الباك هيكون تخمين خطر (أطوال أرقام متفاوتة، أكواد محافظات)؛ النص في `apply/page.tsx` (رسالة الخطأ + التلميح) و`docs/EXHIBITION_PORTAL_SPEC.md` عدّلوا لإزالة ذكر الأرضي، مع توضيح إنه نفس نمط تحقق تسجيل الدخول |
| FND-036 | P1 | `apps/admin/src` و`apps/dealers/src` بالكامل، `packages/api-client/src/mock/db.ts::getHealth` | `MISSION.md` مرحلة ١ (توجيه صريح لاستخدام `nowMs()`)، طلب المستخدم الصريح "حل كل المشاكل الي اتذكرت" | **صفر استخدام لـ`nowMs()` في كل لوحة الأدمن** (بعكس `apps/dealers` اللي كانت بتستخدمها صح في موضعين بس هي كمان) — كل الأزمنة النسبية (`relTimeAr`, `waitingFor`, `hoursSince`, `secondsUntil`) بتعتمد على `new Date()` الحقيقية بدل ساعة الموك المجمَّدة (`MOCK_NOW`). | **اتصلّح** — تصميم مقصود بتمييز واضح بين نوعين: (١) **الأزمنة النسبية لبيانات ثابتة/محسوبة SLA** (آخر ظهور، مستني بقاله، سجل التدقيق، أيام العقد المتبقية، تاريخ التقديم...) — دي بقت كلها بتستخدم `now = new Date(nowMs())` بدل `new Date()` الافتراضية، في كل صفحات الأدمن (`users`, `sell-now`, `sell-now/[id]`, `listings/[id]`, `health`, `financing`, `financing/[id]`, `exhibitions`, `exhibitions/[id]`, `audit`, `exhibitions/requests`) وصفحات المعارض (`billing`, `contract`, `leads`, `apply/status`)، ومقاييس SLA جوه `mock/db.ts::getHealth` (طابور الفحص، الفشل آخر ٢٤ ساعة، العروض/الإعلانات المنتهية). (٢) **نظام المزادات اللايف عن قصد استُثني** (`auctions/*`, `auctions/[id]/*` في التطبيقين، عدّاد المزاد، الووركر التلقائي، `countOverdueAuctions`) — دول محتاجين وقت حقيقي متقدّم فعلًا عشان العدّاد التنازلي والتمديد ضد القنص يفضلوا شغالين حقيقي، وتجميدهم كان هيكسر المزايدة اللايف (مؤكّد بالتحليل قبل التنفيذ، مش تخمين). مؤكّد بالكامل: `npm run typecheck` (صفر أخطاء) + `npm run test` (١٤٤/١٤٤ ناجحة) |
| FND-037 | P1 | `apps/admin/src/app/(dash)/audit/page.tsx`، `packages/api-client/src/mock/db.ts::queryAudit` | `ADMIN §4.9`، `ADMIN §6.3` بند ٣ (`?from=&to=&actor_id=`) | فلترة "الفاعل" كانت فعليًا بحث نصي في الصفحة المعروضة بس (٣٠ صف)، مش على السجل كله. فلترة "التاريخ" (`from`/`to`) غير موجودة خالص. | **اتصلّح في وضع الموك** — `queryAudit` بقت بتاخد `actor`/`from`/`to` كمعاملات وتفلتر `db.auditLog` كله **قبل** الترقيم (مش بعده)؛ الصفحة بقى فيها حقل "الفاعل" مستقل + "من تاريخ"/"إلى تاريخ" (`<input type="date">`) بيتبعتوا سيرفر-سايد، بدل ما يتلخبطوا مع مربّع "بحث" العام. `from`/`to` بيتحوّلوا للحظة UTC الصحيحة بتوقيت القاهرة (`Intl.DateTimeFormat` مدمجة — بلا مكتبة خارجية، لاحظنا `date-fns-tz` بتسبب timeout حقيقي ٥+ ثواني في أول تحميل جوه Vitest على الجهاز ده فرجعنا عنها). مربّع "بحث" فضل بس للسبب/رقم الكيان/اسم الأكشن على الصفحة المعروضة — حد موثّق صراحة في تلميح الشاشة. `scripts/mock-backend.mjs` (سيرفر الاختبار المصغّر) **اتسيب عمدًا من غير** فلترة حقيقية — أي endpoint فيه أصلًا بيرجّع القائمة كاملة بغضّ النظر عن الـquery، ده نطاقه الرسمي المعلن في تعليق الملف. مؤكّد: `npm run typecheck` + `npm run test` (١٥١/١٥١، شامل ٧ اختبارات جديدة لـ`queryAudit` في `audit-filters.test.ts`) + `npm run lint` (صفر تحذيرات). الباك الحقيقي لسه لازم يطبّق نفس الفلترة سيرفر-سايد — موثّق في `ADMIN §6.3` بالفعل |
| FND-038 | P2 | `apps/admin/src/components/Shell.tsx` | `ADMIN §4.8` (بانر متأخرة عن القفل "في كل الصفحات") | النص الحرفي بيطلب بانر أحمر كامل العرض لمزادات متأخرة عن القفل في كل صفحات اللوحة. المنفَّذ فعليًا: badge رقمي في الشريط الجانبي على كل الصفحات + بانر نصي كامل بس في ٤ صفحات مباشرة (`/`, `/auctions`, `/auctions/[id]`, `/health`). الـbadge بيوصّل نفس المعلومة عمليًا لكن مش بشكل "بانر" زي النص. | **اتصلّح** — بانر عالمي أحمر في `Shell.tsx` (كل الصفحات، مخفي في `/` بس لأنها عندها نفس البانر بتفاصيل إضافية) بدل ما البانرات المحلية الأربعة تفضل هي المصدر الوحيد؛ البانرات المحلية سيبت زي ما هي (تكرار بصري بسيط، مقبول، مش مكسور) |
| FND-039 | P3 | `apps/dealers/src/app/(portal)/page.tsx` | `PORTAL §7` (D-04 استفسارات/يوم) | رسم "استفسارات/يوم" (D-04) من كتالوج تشارتس البوابة غير موجود خالص في لوحة المعرض الرئيسية — بحث نصي كامل رجّع صفر. البيانات المصدر (`useMyLeads`) متاحة بالفعل. | مفتوح — إضافة `ChartFrame` بنفس نمط D-01 ممكنة، لكن خارج نطاق "إصلاح صغير" الآمن لهذه المرحلة تحديدًا (يحتاج قرار عن مكان الرسم في التخطيط) |
| FND-040 | P3 | متفرقات صغيرة غير حرجة | متنوع | (أ) D-06 (متوسط أيام حتى البيع) بلاطة من غير sparkline فعلي — الكود يقر بغياب `sold_at` من الباك. (ب) فورم `/apply` بيفحص حجم الملف بس، مش نوعه الفعلي (MIME) بعد الاختيار. (ج) C-51 (عمر أقدم مهمة queued) موجود بالمضمون في `/health` بدون توسيم `code="C-51"` صريح زي باقي التشارتس. (د) محور ساعات C-52 Heatmap معكوس اتجاهيًا (٢٣→٠ بدل ٠→٢٣) بعكس عُرف "الأقدم شمال" المتّبع في باقي المحاور الزمنية. (هـ) `GET /v1/auctions` في `dealers/hooks.ts` بدون `?limit=50` المذكور في مثال المواصفة (انحراف نصي بسيط، غير حرج). (و) حالة `SN` `cancelled` مدعومة بالكود لكن غير ممثَّلة في بيانات الـseed. | مفتوح — كل واحدة منها تجميلية/هامشية، مسجَّلة للمتابعة المستقبلية بدون أولوية عاجلة |
| FND-041 | P1 | `packages/api-client/src/admin/hooks.ts` (١٠ مواضع) | X-3، `MISSION §3.6` بند ٤ | عشرة mutations في `admin/hooks.ts` (`useCollectSellNow`, `useSetListingFlags`, `useSetListingStatus`, `useSetContract`, `useReviewApplication`, `useMarkEntryPaid`, `useMarkDefaulted`, `useSetFinancingStatus`, `useSetUserRole`, `useSetUserStatus`, `useRevealPhone`) كانت من غير `Idempotency-Key` خالص. | **اتصلّح في المرحلة ٥** — نفس نمط `IdempotencyKeyCache` (`FND-006`) اتطبّق على كل الـ١٠، تفاصيل المفاتيح في `docs/BACKEND-CONTRACT.md §6.4` |
| FND-042 | P2 | `packages/api-client/src/client.ts` | `MISSION.md §5.5d` | مفيش `X-Request-Id` على أي نداء — طلب فاشل ملوش أي معرّف يتلاقى بيه في لوج الباك، والمستخدم مايشوفش رقم مرجع في رسالة الخطأ. | **اتصلّح في المرحلة ٥** — كل نداء بيبعت `X-Request-Id` (UUID)، بيتلحق تلقائيًا برسالة `errorMessage()`، ومدعوم إن الباك يرجّع معرّفه هو لو موجود. لُقط أثناء التجربة الحية باج مرتبط: CORS في `mock-backend.mjs` كان ناسي يسمح بالهيدر ده — اتصلّح |
| FND-043 | P3 | `reports/FINDINGS.md` (قسم الإحصائية) | — | تناقض داخلي: صف `FND-034` معلّم "اتصلّح" لكن سطر الملخّص تحته بيقول لسه أخطر ملاحظة مفتوحة، وفيه سطر إحصائية زايد من مسودة قديمة متبقّي. | **اتصلّح في المرحلة ٥** — الملخّص اتعاد حسابه بالكامل من الصفوف الفعلية |
| FND-044 | P2 | `packages/api-client/src/admin/hooks.ts:499-507` (`useAuctionEntries`) | `ADMIN §6.2` (`GET /v1/admin/auction-entries?paid=false&cursor=`) | الـhook بيرجّع `AuctionEntry[]` خام (مش `Page<AuctionEntry>`) ومفيش `cursor` بيتبعت خالص — مخالف لعقد المواصفة نفسه. مستخدم من غير `auctionId` (كل الصفوف، عبر المنصة كلها) في `/auctions` (جدول رسوم غير مدفوعة) و`/exhibitions` و`/exhibitions/[id]` (لحساب حالة دفع كل معرض) — الاستخدامين الأخيرين محتاجين **كل** الصفوف للتجميع عبر معارض، مش صفحة واحدة، فمجرد إضافة `cursor` هيكسرهم. | مفتوح — **قرار تصميم مطلوب من الباك**: naive pagination مش كافية هنا؛ الأصح endpoint تجميعي (`GET /v1/admin/exhibitions/{id}/unpaid-entries-count` أو مشابه) بدل جلب كل الصفوف للفرونت. موثّق في `docs/BACKEND-CONTRACT.md` |
| FND-045 | P3 | تكرار الطلبات (query keys) | `MISSION §5.5e` | فحص حي (Chrome، تسجيل شبكة كامل أثناء اختبار التبديل للباك اند الحقيقي) — مفيش أي مورد اتطلب أكتر من مرة في نفس اللحظة لنفس الشاشة. TanStack Query v5 بيعمل hash للـquery key بالتسلسل (serialization) مش بمرجع الكائن (reference identity)، فكائن فلاتر جديد كل render **مش بيكسر** الـdedup زي ما كان متوقّع نظريًا — طالما القيم متطابقة. | **اتفحص ومفيش مشكلة** — تأكيد تجريبي لا يحتاج إصلاح؛ فحص شامل لكل شاشة على حدة (بعكس العيّنة اللي اتفحصت هنا) لسه ممكن يتعمل في مرحلة ٧ |
| FND-046 | P1 | `security-headers.mjs` | DEPLOYMENT.md («HTTPS إجباري + HSTS») | `Strict-Transport-Security` و`upgrade-insecure-requests` كانوا بيتبعتوا تلقائيًا بمجرّد `NODE_ENV=production`، من غير أي تأكّد إن فيه TLS فعلي ورا السيرفر. اتلقطت حيًا: `next build && next start` (بلا TLS، زي أي استضافة Node مباشرة) بيدّي المتصفح الهيدرين، فبعدها أي نداء تاني لنفس الأصل (حتى prefetch داخلي من Next نفسه) بيتفرض عليه HTTPS ويفشل بـ`ERR_SSL_PROTOCOL_ERROR` — يعني السيرفر بيقفل نفسه بنفسه. التعليق القديم في الكود كان بيقول "HSTS مالهوش أثر مع HTTP" — ده غلط فعليًا (السلوك ده جوه المتصفح مش السيرفر). | **اتصلّح في المرحلة ٥** — الهيدرين دلوقتي مشروطين بـ`isHttpsDeployment` (`process.env.VERCEL` أو `FORCE_HTTPS_HEADERS=true` صراحة) بدل `!isDev` بس. موثّق في `docs/SECURITY-REVIEW.md` و`DEPLOYMENT.md`. مؤكّد بإعادة اختبار معزولة (٢/٢ نجحوا) + السويت الكامل (١١٤ نجحوا + ١ flaky غير مرتبط، صفر فشل حقيقي) |
| FND-047 | P2 | `playwright.config.ts` (`webServer`) | — | `npm run e2e` كان بيشغّل `next dev` (وضع تطوير، webpack بيcompile كل route عند أول زيارة) تحت ٢ Playwright workers بيضربوا ٢٠+ مسار بالتوازي — استهلاك ذاكرة ٤.٥+ GB (من أصل ١٥.٤ GB متاحة، ٢ GB فاضيين بس)، وقت تشغيل ٣٦ دقيقة، وبطء/تعليق ملحوظ. | **اتصلّح جذريًا في المرحلة ٥** — `webServer` بقى بيبني وبيشغّل نسخة إنتاجية (`next build && next start`) بدل `next dev`؛ الذاكرة نزلت لـ~٣٨٣ MB، ووقت التشغيل الكامل نزل من ٣٦ دقيقة لـ٨.٤ دقيقة (٤.٣×). التكلفة الوحيدة: تعديلات حيّة في الكود محتاجة إعادة تشغيل `npm run e2e` (مفيش live-reload)، موثّقة في `README.md` |
| FND-048 | P3 | `packages/api-client/src/dealers/hooks.ts` (`useMyExhibition`) | `MISSION §6` بند ٥ | `isContracted`/`verified` بيتغيّروا من شاشة أدمن تانية بالكامل (`/exhibitions/[id]`)، ومفيش `refetchInterval` خالص على `useMyExhibition` — المعرض ميشوفش تعاقده اتفعّل غير لما يعمل focus على التاب (`refetchOnWindowFocus`) أو يفتح صفحة تانية. مفيش موضوع WS موصوف للتغيير ده (`docs/REALTIME.md §4`). | **اتصلّح في المرحلة ٦** — `refetchInterval: visibleRefetchInterval(60_000)` (نفس نمط `useExhibitionStats` الموجود بالفعل) — بولينج خفيف، مش موضوع WS مخترَع |
| FND-049 | P3 | موضوع `chat:{thread_id}` | `R-1`، `MISSION §6` بند ٥ | الموضوع موجود فعليًا في الباك، لكن شكل حمولة حدث الرسالة **مش موصوف** في أي مواصفة وصلتنا (خلاف `auction:{id}` اللي `PORTAL §4.5` بتوصفه بالحرف). | مفتوح — `useLeadMessages` بتتعامل مع أي حدث على الموضوع كـ"اسحب تاني" (إبطال كاش عام) بدل قراءة حمولة مخترَعة، وبولينج خفيف (`visibleRefetchInterval`) شبكة أمان. **سؤال صريح للباك** في `docs/BACKEND-CONTRACT.md §7` بند ٦ |
| FND-050 | P3 | كل `refetchInterval` في `admin/hooks.ts`+`dealers/hooks.ts` | `MISSION §6` بند ٦ | مفيش أي polling في المشروع كان بيوقف لما التاب مش ظاهر — تاب مقفول فاتح على ٢٠ معرض بيعمل نداءات شبكة مجانية للأبد. | **اتصلّح في المرحلة ٦** — `visibleRefetchInterval(ms)` جديدة في `client.ts`، مطبّقة على كل `refetchInterval` في التطبيقين (`POLL.*` في الأدمن، وكل بولينج مشابه في المعارض) |
| FND-051 | P2 | `playwright.config.ts` | — | `e2e/realtime.spec.ts` الجديد (محتاج `mock-backend.mjs`+`NEXT_PUBLIC_API_URL`) اتلقط تلقائيًا بـ`testDir:'./e2e'` الافتراضي وجري فعليًا مع `npm run e2e` العادي (وضع الموك) — فشل حتميًا لأن كل سياق متصفح في الموك معزول (مفيش حالة مشتركة بين تابين). اتلقط بتشغيل `npm run e2e` الكامل بعد التعديل (٢ فشل من ١١٧). | **اتصلّح فورًا في نفس المرحلة** — `testIgnore: /realtime\.spec\.ts/` في `playwright.config.ts` الرئيسي؛ الملف بيشتغل بس عن طريق `npm run e2e:realtime` (كونفيج منفصل). مؤكّد بإعادة تشغيل `npm run e2e` كامل بعد الإصلاح |
| FND-052 | P0 | `packages/api-client/src/mock/db.ts::getOwnedListing`، `scripts/mock-backend.mjs::findOwnedListing` (و`GET/PATCH/DELETE /v1/listings/{id}` في الباك الحقيقي) | `MISSION §7` — «اختبارات الحدود (IDOR)» | **IDOR كان مؤكّد باختبار حي:** معرض داخل بجلسة `ex-1` كان يفتح `/inventory/{id}` بتاع إعلان معرض تاني (بتغيير الـid في الـURL بس) ويشوف بياناته كاملة، من غير أي رسالة رفض. `getListing(id)` الخام في `mock/db.ts` كانت بترجع أي إعلان بمجرد وجوده، بلا فحص `seller.id` أصلًا. | **اتصلّح في وضعي الموك الاتنين** (`getOwnedListing` في `mock/db.ts` — مستخدمة في الـ٦ مسارات دلوقتي بدل `getListing` الخام؛ `findOwnedListing` في `mock-backend.mjs` بترجع `403 FORBIDDEN` لأي طلب مش من صاحب الإعلان). مؤكّد باختبار حي محدّث (`idor.spec.ts`) + اختبار عقد آلي جديد (`contract.test.ts`). **الباك الحقيقي لازم يطبّق نفس الفحص سيرفر-سايد** — النسخة هنا مرجع تنفيذي، موثّق إلزاميًا في `docs/BACKEND-CONTRACT.md §6.0`. `getListing` الخام باقية بلا فحص لاستخدام الأدمن الشرعي (يشوف كل حاجة بحكم الدور) |
| FND-053 | P3 | `mock/seed.ts` (`l-{n}`, `ex-{n}`, `a-{n}`, `sn-{n}`) | `MISSION §7` | كل الـids في بيانات الموك متسلسلة رقميًا وسهلة التخمين (`l-1`..`l-190` إلخ) — موثّق كملاحظة صراحة في المواصفة نفسها («لو الـids متوقّعة، سجّلها كملاحظة للباك»)، مش فشل اختبار. | مفتوح — **ملاحظة للباك بس، مش عيب فرونت.** الأمان الحقيقي لازم يعتمد على فحص الصلاحية (`FND-052`) مش على صعوبة تخمين الـid — لو الباك عايز UUIDs عشوائية بدل تسلسل، ده تحسين إضافي مش بديل عن `FND-052` |
| FND-054 | P3 | `packages/ui/src/charts/Charts.tsx::Funnel` | `MISSION §7` (مراجعة سكرينشوتس بالعين) | مراجعة سكرينشوت `قمع النشر` (admin overview) وريّت نص شكله «١٦١٨٥٪ من اللي قبلها» — شكّ أول إنه باج حسابي حقيقي (فحصته حيًا بمتصفح فعلي وقارنت الأرقام يدويًا قبل ما أفترض باج: `190→161(٪85)→80(٪50)→25(٪31)→13(٪52)` — الحساب صح ١٠٠٪. المشكلة كانت إن عدد الخطوة ونسبتها متلاصقين في الـDOM (مفيش حرف فاصل، بس `margin` بصري) — أي نسخ نص أو screen reader بيقراهم كرقم واحد "١٩٠٨٥٪". | **اتصلّح** — فاصل صريح (`' · '`) قبل النسبة بدل الاعتماد على margin بصري بس. مؤكّد حيًا (get_page_text قبل/بعد) والحساب الأصلي كان سليم من البداية — **مفيش باج بيانات حقيقي**، بس تحسين وضوح/a11y حقيقي |
| FND-055 | P2 | `packages/api-client/src/mock/db.ts::signIdImage` | `MISSION §7` — اختبار «عرض صورة موقّتة» في `/financing/[id]` | الرابط الموقّع اللي الموك بيرجّعه لصورة البطاقة كان بيوّدي على `/api/private-preview?key=...` — مسار مالوش أي route handler خالص في `apps/admin`. النتيجة: ديالوج «وش/ظهر البطاقة» بيفتح صح (العدّاد شغّال)، لكن الصورة نفسها كانت بترجع 404 دايمًا، والـ`onError` بيتفعّل صامت (`imageFailed`) — يعني الفيتشر كله كان مبني وشغّال ماعدا نص المتر الأخير. اتلقط بمحاولة أول لاختبار حي للزرار. | **اتصلّح** — بدل ما نبني route جديد (اللي هيحتاج ألوان لعرض placeholder، وده بيقع في `no-hex-in-pages` — اتلقط ده كمان أثناء محاولة الإصلاح الأولى)، رجّعنا رابط لأصل ثابت (`apps/admin/public/id-card-placeholder.svg`). أبسط وأصح مع قواعد المشروع |
| FND-056 | P1 | `packages/api-client/src/mock/db.ts` (ووركر إقفال المزادات، المرحلة ٦) | `mock/seed.ts` (`overdue = isLive && i === 0`) | ووركر إقفال المزادات التلقائي اللي اتضاف في المرحلة ٦ (`setInterval` كل ٥ث بيقفل أي مزاد `live` عدّى `endsAt`) كان بيقفل حتى `a-1` — المزاد المتعمّد يفضل «متأخر عن القفل للأبد» عشان يوري بانر «العامل الخلفي واقف» في `/health` وكل شاشة أدمن (فيكستشر من المرحلة ٠). النتيجة: بعد ساعات من تشغيل نفس السيرفر (مؤكّد فعليًا — `admin.spec.ts` فشل بعد جلسة طويلة لأن تاب «شغالة» بقى فاضي تمامًا)، كل المزادات الـlive في العيّنة اتقفلت وكسرت اختبار موجود من قبل المرحلة ٦. | **اتصلّح** — استثناء صريح لـ`a-1` (`PERPETUALLY_OVERDUE_DEMO_AUCTION_ID`) من الووركر الجديد. باقي المزادات الـlive (i=1..5) لسه بتتقفل عادي — الفيكستشر المتعمّدة بس هي المستثناة |
| FND-057 | P2 | `apps/{admin,dealers}/package.json` (`dev` script) | طلب صريح من المستخدم — "أي صفحة بتفتح تقيلة جدًا ووقت انتظار كبير" | `next dev` الافتراضي (webpack) بيترجم كل route عند أول زيارة — نفس السبب الجذري اللي اتشخّص في المرحلة ٥ لبطء `e2e`، بس هنا بيأثر على تجربة التطوير اليومية نفسها (مش بس اختبارات آلية). جهاز المستخدم كان عليه وقت الفحص ٣١ عملية Chrome شغّالة و٤.٦ جيجا رام فاضية بس من أصل ١٥.٨ — ضغط ذاكرة حقيقي بيكبّر المشكلة. | **اتصلّح جزئيًا** — `next dev --turbopack` بدل webpack في التطبيقين (مؤكّد حيًا: نفس اختبار `e2e:realtime` نزل من ~٢.٢ دقيقة لـ١.٥ دقيقة). أول زيارة لأي route لسه بتاخد وقت (طبيعة أي dev server JIT — مش حاجة Turbopack بيحلّها)؛ لسه محتاج `npm run preview:admin`/`preview:dealers` (نسخة إنتاجية، سكريبتين جداد) للتصفّح العادي من غير تعديل كود — موثّق في `README.md` |
| FND-058 | P2 | `packages/ui/package.json` | طلب صريح من المستخدم — "عايز الاستهلاك بتاع الموقع خفيف جدًا زي أي موقع في الدنيا" (بعد `FND-057`، لأن سرعة الـdev مش نفسها خفّة الإنتاج الفعلية) | `@carq/ui` بيتصدّر من نقطة واحدة (`src/index.ts` كـbarrel) وكان من غير حقل `sideEffects` في `package.json` — الافتراضي بالنسبة لـwebpack/Turbopack في الوضع ده إنه يفترض كل الملفات ممكن يكون فيها side effects، فبيسحب كل حاجة في الباكيدج (شامل `recharts` وكل مكوّنات الرسوم البيانية، `Charts.tsx`/`ChartFrame`/`theme.ts`) جوه أي صفحة بتعمل import لأي حاجة من `@carq/ui` — حتى صفحة زي `/login` اللي مالهاش أي علاقة بالرسوم البيانية. اتأكّد بالأرقام: كل صفحة في التطبيقين كانت بتحمّل ~٢٨٠-٢٩١ كيلوبايت "First Load JS" حتى الصفحات البسيطة (`/login`, `/users`, `/profile`...). | **اتصلّح** — إضافة `"sideEffects": false` في `packages/ui/package.json` (بعد التأكّد بالبحث إنه مفيش أي import من نوع side-effect-only أو استيراد CSS خام جوه `packages/ui/src/**`). النتيجة مؤكّدة ببناء إنتاجي كامل قبل/بعد للتطبيقين: كل صفحة من غير رسم بياني نزلت **~٣٩-٤٦٪** (مثال أدمن: `/login` ٢٨٠→١٥١ كيلوبايت، `/users` ٢٨٢→١٦٩، `/audit` ٢٨٨→١٦٧؛ مثال معارض: `/login` ٢٨٣→١٥٥، `/inventory` ٢٨٠→١٦٩، `/profile` ٢٨١→١٦٥). الصفحات اللي فعلًا محتاجة رسوم بيانية (`/`, `/auctions`, `/health`...) فضلت زي ما هي تقريبًا — طبيعي وصحيح، لأنها فعلًا محتاجة الكود ده. `packages/api-client/package.json` **اتسابت عمدًا من غير** `sideEffects: false` لأن فيها side effects حقيقية وقت التحميل (ووركر إقفال المزادات في `mock/db.ts`، والـ`wsConnection` singleton في `realtime.ts`) — تعليمها غلط ممكن يمسح كود شغّال فعليًا بصمت. مؤكّد بالكامل: `npm run verify` (كل الخطوات الـ٨ نجحت) + `npm run e2e` كامل (١٤٦/١٤٦ نجحوا، صفر تغيير سلوكي) |
| FND-059 | P2 | `apps/{admin,dealers}` (`instrumentation-client.ts`, `sentry.*.config.ts`, `src/lib/sentry-shared.ts`, `src/lib/report-error.ts`) | `MISSION.md` مرحلة ٨ بند ٥ — "صفر أداة رصد أخطاء" | صفر أداة تتبّع أخطاء إنتاج في المشروع كله. لو شاشة وقعت عند معرض أو أدمن حقيقي، محدش هيعرف غير لو المستخدم اشتكى بنفسه. | **اتصلّح** — `@sentry/nextjs` مضاف في التطبيقين (موافق عليه صراحة مع المستخدم). **درس مهم اتكشف أثناء التنفيذ:** التحميل المباشر (`Sentry.init` وقت الاستيراد) بيضيف ~٧٠-٨٠ كيلوبايت لـ"First Load JS" **لكل صفحة** — كان هيلغي نص تحسين `FND-058` ويتعارض مباشرة مع طلب المستخدم الصريح عن الخفة. الحل: `import()` ديناميكي مؤجّل لحد ما المتصفح يفضى (`requestIdleCallback`) في `instrumentation-client.ts` **و**في `report-error.ts` (المستخدم من `error.tsx` — لازم يتحمّل مع كل صفحة كـerror boundary، فاستيراد Sentry منه مباشرة كان بيلغي فايدة التأجيل). مؤكّد بالأرقام: بعد التأجيل، "First Load JS" رجع بالظبط لأرقام `FND-058` (`/login` ١٥٢ كيلوبايت، الأساس المشترك ١٠٤ كيلوبايت) — صفر تأثير ملحوظ. المقايضة الوحيدة المقبولة: خطأ في أول ثانية-اتنين من التحميل ممكن ميترصدش. الخصوصية: `sendDefaultPii: false`، `beforeSend` بيسكرب أي رقم تليفون مصري ظاهر بالغلط، و`integrations` بترفض `Replay` صراحة (مش الاعتماد بس على معدل عيّنة صفري). بلا `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` الـSDK بيتحمّل بس مبيبعتش حاجة — نفس مبدأ مرونة جوجل أوث. تفاصيل كاملة في `docs/OBSERVABILITY.md`. مؤكّد: `npm run verify` + `npm run e2e` (١٤٦/١٤٦) + فحص حي بمتصفح فعلي (تأكيد التحميل الكسول وعدم إرسال أي حدث بلا DSN) |
| FND-060 | P2 | `apps/{admin,dealers}/src/app/(dash\|portal)` | `MISSION.md` مرحلة ٨ بند ٣ (المتانة) — "`error.tsx` و`loading.tsx` لكل route group — موجودين؟" | صفر `error.tsx` وصفر `loading.tsx` في التطبيقين — الموجود الوحيد `not-found.tsx` على مستوى الجذر. أي خطأ رندر غير متوقع في أي صفحة كان هيطلّع شاشة بيضا/كراش React الافتراضي بدل حد أمان بيسيب باقي اللوحة شغّالة. | **اتصلّح** — `error.tsx` (Client Component، بينادي `reportError()` من `FND-059` في `useEffect`) و`loading.tsx` (شكل عام بـ`Skeleton`) لكل من `(dash)` و`(portal)`. اتحطّوا **جوه** الـroute group عمدًا — الـ`layout.tsx` (وبالتالي `Shell`: الشريط الجانبي والهيدر) بيفضل شغّال حواليهم، فرسم بياني واحد أو صفحة واحدة بتقع مايوقّعش الداشبورد كله. مؤكّد: `npm run typecheck` + بناء إنتاجي ناجح للتطبيقين + `npm run e2e` كامل |
| FND-061 | P3 | `packages/ui/src/components/DataTable.tsx` (وكل الـ٢٢ استخدام في التطبيقين) | `MISSION.md` مرحلة ٨ بند ١ — "الجداول: `<th scope>` و`caption` أو بديل" | `<th scope="col">` كان موجود بالفعل (شغل مراحل سابقة)، لكن الـ`<table>` نفسه مالوش اسم واضح لقارئ الشاشة (لا `caption` ولا `aria-label`) — لو مستخدم قارئ شاشة نقل لوضع "استعراض كل الجداول في الصفحة" (خاصية شائعة في JAWS/NVDA)، كل جداول المشروع هتظهر بلا اسم يميّزها عن بعض. | **اتصلّح** — `caption?: string` جديد في `DataTableProps`، بيترندر كـ`<caption className="sr-only">` (مخفي بصريًا، موجود لقارئ الشاشة بس). اتضاف لكل الـ٢٢ استخدام في التطبيقين بنص عربي واضح لكل جدول ("جدول المستخدمين"، "جدول المزايدات"، إلخ). مؤكّد: `npm run typecheck` + `npm run e2e` كامل (سكرينشوتس الشاشات كلها فضلت زي ما هي — تغيير مخفي بصريًا بس) |
| FND-062 | P3 | `packages/ui/src/charts/Charts.tsx` | `MISSION.md` مرحلة ٨ بند ٦ (نظافة الكود) — "`any` في TS ... كل واحدة تتبرر أو تتشال" | ٣ استخدامات لـ`any` (بمعاها `eslint-disable`) في دوال tooltip الرسوم البيانية — مبررة بتعليق ("recharts types صعبة") بس لسه ممكن تتشال بالنوع الصح. | **اتصلّح** — استُبدلت بـ`TooltipProps<ValueType, NameType>` المصدَّرة فعليًا من `recharts` (لدوال الـtooltip العامة)، وبنوع محلي (`ScatterPoint`) لنقطة البيانات في `ScatterPlot` (اللي `recharts` نفسها بترجّعها `any` في تعريفها الداخلي — كاست واحد موضّح بتعليق بدل `any` منتشر). صفر `eslint-disable`/`any` باقي في الملف. مؤكّد: `npm run typecheck` + `npm run lint` (صفر تحذيرات) + `npm run e2e` (سكرينشوتس الرسوم البيانية كلها زي ما هي) |
| FND-063 | P2 | `package.json` (جذر المشروع، حقل `overrides`) | `FND-015` — شق الـ`postcss` تحديدًا | نسخة `postcss` الداخلية جوه `next` (`8.4.31`) فيها ٤ ثغرات **"عالية"** موثّقة (XSS في stringify، وتلاتة صيغ لقراءة ملفات `.map` عشوائية عن طريق `sourceMappingURL` متحكَّم فيه). النسخة الأحدث المتاحة (`8.5.28`) بتصلّحهم كلهم — **بس جوه نفس الخط الرئيسي (`8.x`)**، عكس `next`/`vitest` (قفزة رئيسية). | **اتصلّح بأمان صفري المخاطر** — `"overrides": { "postcss": "^8.4.49" }` في `package.json` الجذر، بيجبر كل نسخ `postcss` في الشجرة (شامل نسخة `next` الداخلية المثبّتة صراحة `8.4.31`) على `8.5.28` من غير أي تغيير في `next` نفسه أو أي كود مشروع. محتاج إعادة تثبيت كاملة (`rm -rf node_modules package-lock.json && npm install`) عشان الـoverride يتطبّق فعليًا على النسخ المتداخلة — تثبيت عادي (`npm install`) وحده مايكفيش. مؤكّد: النسخة الداخلية جوه `node_modules/next` اختفت خالص (اندمجت في نسخة واحدة `8.5.28` في الشجرة)، `npm audit` نزل من ٧ ثغرات لـ٥ (الأربعة "عالية" اختفوا بالكامل)، `npm run verify` (كل الـ٨ خطوات) + `npm run e2e` كامل (١٤٦/١٤٦) بعد التثبيت النظيف |

---

## إحصائية سريعة (آخر تحديث — بعد `FND-063`)

| الخطورة | مفتوح | مُقفلة (اتصلّحت/اتحلّت) |
|---|---|---|
| P0 | ٠ | ٤ |
| P1 | ٠ | ١٥ |
| P2 | ٥ | ٢٠ |
| P3 | ١٠ | ٩ |
| **الإجمالي** | **١٥** | **٤٨** |

**صفر ملاحظة P0 أو P1 مفتوحة حاليًا.** آخر P0 (`FND-052`، IDOR) اتصلّحت
في وضعي الموك الاتنين، والفحص المطلوب من الباك الحقيقي موثّق إلزاميًا في
`docs/BACKEND-CONTRACT.md §6.0`. آخر P1 (`FND-036` نظام الوقت المجمّد،
و`FND-037` فلترة سجل التدقيق) اتصلّحوا الاتنين بطلب صريح من المستخدم
("حل كل المشاكل الي اتذكرت").

باقي الـ١٥ ملاحظة المفتوحة كلها P2/P3: إما فجوات توثيق أو تنظيف كود مؤجّلة
لمرحلة ٨، أو بنود محتاجة endpoint/قرار من فريق الباك (موثّقة في
`docs/BACKEND-CONTRACT.md`، بما فيها `FND-044` — pagination
`useAuctionEntries` محتاجة endpoint تجميعي مش cursor بسيط، `FND-049` —
شكل حدث رسالة المحادثة على `chat:{thread_id}` مش موصوف (`docs/REALTIME.md`)،
و`FND-053` — ids متسلسلة/سهلة التخمين، ملاحظة توثيقية بس مش عيب)، أو
`FND-015` (`npm audit` — شق `vitest`/`esbuild`/`vite` بس، شق `postcss`
اتصلّح فعليًا في `FND-063`) اللي **قرارها اتاخد صراحة: تتساب زي ما هي
دلوقتي** — الترقية الوحيدة المتاحة قفزة `vitest` مجورين رئيسيين (مش
تُرقّى breaking قبل ما يبقى فيه باك اند حقيقي متوصّل ووقت لإعادة تحقق
كامل للاختبارات).

التفاصيل الكاملة لكل ملاحظة والدليل الداعم لها في تقارير كل مرحلة
(`reports/PHASE-*.md`) و`reports/SPEC-MATRIX.md`. كل الملاحظات دي **مؤكدة
بقراءة كود مباشرة أو بأمر/اختبار شغّال فعليًا** — مفيش ادعاء بلا دليل.
