# MISSION.md — مهمة المراجعة والتجهيز الكاملة لمشروع CarQ Web

> **للنموذج المنفّذ (Claude Code):** الملف ده هو التعليمات الحاكمة للمهمة كلها.
> اقراه بالكامل قبل ما تكتب أي سطر كود. ولو أي تعليمة في المحادثة اتعارضت مع
> الملف ده، الملف ده هو المرجع — إلا لو المستخدم قال صراحةً «تجاهل MISSION.md في النقطة دي».

---

## ١. إنت مين وبتعمل إيه

إنت Staff Engineer داخل فريق CarQ، مكلّف بمراجعة ومعالجة الـmonorepo بتاع الويب
(`carq-web`) قبل ما يتربط بالباك اند وبتطبيق الموبايل ويتنشر.

المشروع دلوقتي: **واجهتين Next.js شغّالين بالكامل على طبقة موك**، مبنيين على
مواصفات مكتوبة بالتفصيل. الشغل المطلوب منك مش «اكتب فيتشرز جديدة» — الشغل هو:

1. **تتحقق** إن اللي متبني مطابق للمواصفة بالحرف، ومفيش زرار واحد ميت.
2. **تبني** الطبقات الناقصة فعليًا (الأوثنتيكيشن، الصلاحيات، طبقة الريل تايم).
3. **تشدّ** الأمان لغاية مستوى منتج فيه بيانات شخصية تحت قانون ١٥١/٢٠٢٠ المصري.
4. **تجهّز** المشروع للربط بالباك اند وبتطبيق الموبايل من غير إعادة كتابة.
5. **تختبر** كل شاشة وكل زرار وكل حالة بدليل — مش بادعاء.

المهمة مقسّمة لـ**١٠ مراحل**. **متعملش أكتر من مرحلة واحدة في المرة.** بعد كل
مرحلة بتكتب تقرير وبتقف وتستنى موافقة صريحة.

---

## ٢. خريطة المشروع (اقرا ده قبل ما تفتح أي ملف)

```
carq-web/
├── apps/
│   ├── admin/      داشبورد مالك CarQ    — Next.js 15 App Router — port 3100
│   │   └── src/app/
│   │       ├── (dash)/   / · /sell-now · /listings · /exhibitions ·
│   │       │             /exhibitions/requests · /auctions · /financing ·
│   │       │             /users · /health · /audit  (+ صفحات [id])
│   │       └── login/
│   └── dealers/    بوابة المعارض المتعاقدة — port 3200
│       └── src/app/
│           ├── (portal)/  / · /inventory · /inventory/new · /inventory/bulk ·
│           │              /inventory/[id] · /auctions · /auctions/[id] ·
│           │              /auctions/mine · /leads · /billing · /profile · /contract
│           ├── apply/  ·  apply/status/
│           └── login/
├── packages/
│   ├── ui/                 نظام التصميم المشترك
│   │   ├── src/tokens.ts           ← مصدر الحقيقة الوحيد للألوان
│   │   ├── tailwind-preset.js
│   │   ├── src/components/         PageHeader · DataTable · Dialog · Form ·
│   │   │                           Button · Primitives · StatTile · Feedback ·
│   │   │                           StrokeMotif · Ticker
│   │   ├── src/charts/             ChartFrame · Charts · theme
│   │   └── src/lib/                format · date · csv · cn (+ __tests__)
│   └── api-client/
│       ├── src/types.ts            العقود (≈٥٨٠ سطر)
│       ├── src/client.ts           ★ نقطة التبديل الوحيدة — http() + tokenStore
│       ├── src/errors.ts           ApiError + errorMessage + errorCode
│       ├── src/mock/db.ts          منطق الموك (≈١١٦٠ سطر)
│       ├── src/mock/seed.ts        البيانات (≈٨٠٠ سطر)
│       ├── src/admin/hooks.ts      ≈٤٠ hook
│       ├── src/dealers/hooks.ts    ≈٢٥ hook
│       └── src/__tests__/          rules.test.ts · seed.test.ts
├── e2e/                    admin.spec · dealers.spec · security.spec ·
│                           visual.spec · helpers.ts
├── scripts/                verify.mjs · static-checks.mjs
├── security-headers.mjs    CSP + هيدرز مشتركة للتطبيقين
├── docs/
│   ├── ADMIN_DASHBOARD_SPEC.md      (٧٨٩ سطر)
│   └── EXHIBITION_PORTAL_SPEC.md    (٦٢٠ سطر)
└── DEPLOYMENT.md
```

### أوامر المشروع

| الأمر | بيعمل إيه |
|---|---|
| `npm install` | مرة واحدة من الجذر |
| `npm run dev:admin` | 3100 |
| `npm run dev:dealers` | 3200 |
| `npm run test` | vitest — اختبارات الوحدات (سريعة) |
| `npm run checks` | `scripts/static-checks.mjs` — الأنماط الممنوعة |
| `npm run typecheck` | TS على كل الـworkspaces |
| `npm run verify` | ESLint --fix ← TS ← الوحدات ← الفحوصات الثابتة ← بناء إنتاجي |
| `npm run e2e` | Playwright: يشغّل اللوحتين ويتصرف كمستخدم حقيقي |
| `npm run e2e:report` | تقرير HTML لآخر جولة |

### الباك اند الموجود فعلًا — اقرا ده كويس

`README.md` بيقول «مفيش باك اند لسه». **ده كلام دقيق عن الـendpoints الجديدة
بتاعة الويب بس — مش عن النظام كله.** فيه باك اند Python شغّال بيخدم تطبيق
الموبايل، وفيه منه حاجات كتير جاهزة للاستخدام فورًا:

**الأوثنتيكيشن — موجود بالكامل** (`PORTAL §8.1`):
`POST /v1/auth/otp/request` · `/verify` · `/refresh` · `/logout` · `GET|PATCH /v1/me`

**٨ endpoints أدمن موجودة** (`ADMIN §6.1`) كلهم تحت `/v1/admin` ومحميين
بـ`require_role(ADMIN)` على مستوى الراوتر.

**WebSocket موجود وشغّال:** `{API_URL}/v1/ws` بمواضيع (`R-1`):
`chat:{thread_id}` · `auction:{auction_id}` · `user:{user_id}`.
البروتوكول موصوف بالحرف في `PORTAL §4.5` (الإطارات، المصادقة، الاشتراك،
الأحداث، الـping، تجديد التوكن على سوكت شغّال). **مفيش موضوع `admin:*`** —
ولذلك الـpolling في لوحة الأدمن **قرار مقصود** (`ADMIN §9`) مش نقص.

**مرجع تنفيذي جاهز:** `src/api/auctions.ts::subscribeToAuction` و
`src/api/client.ts` في ريبو الموبايل. المواصفة بتقول صراحة اقراهم كمرجع.

> **نتيجة عملية:** الفجوات اللي هتشتغل عليها هي **فجوات في كود الويب**، مش
> فجوات في النظام. ده معناه إن الشغل وصل ومحدد، مش بحث وتخمين.

### المواصفات هي القانون

أي سلوك في الكود بيشاور على كود قاعدة — `A-1`, `SN-2`, `F-6`, `L-13`, `D-06`,
`C-52`, `X-3`, `I-4` وهكذا — القاعدة نفسها موجودة في `docs/`. **القاعدة هي
المرجع، مش الكود.** لو الكود بيخالف القاعدة، الكود غلط.

أكواد القواعد المستخدمة في المواصفتين (للفهرسة):

* **ADMIN_DASHBOARD_SPEC:** `A-*` `C-01..C-52` `D-*` `F-*` `I-4` `L-5` `P-4` `R-1` `SN-1,2,3,7` `T-1,2` `X-1,2,4,6,8,10`
* **EXHIBITION_PORTAL_SPEC:** `A-0..A-13` `D-01..D-13` `F-6` `I-1,2` `L-3,4,6,7,12,13` `P-4` `R-3,4` `X-3,7`

---

## ٣. القواعد الحاكمة — ممنوع تكسرها في أي مرحلة

### ٣٫١ قواعد الكود

1. **`npm run verify` لازم يفضل أخضر** بعد كل مرحلة. لو مرحلة كسرته، المرحلة مش خلصت.
2. **ممنوع إيموجي** في أي ملف مصدر أو أي واجهة. (`★` U+2605 رمز مطبعي مسموح — المواصفة بتستخدمه.)
3. **ممنوع hex** خارج `packages/ui/src/tokens.ts` و `packages/ui/src/charts/theme.ts`.
   أي لون في الصفحات بيتاخد من التوكنز.
4. **ممنوع `localStorage` / `sessionStorage`** لأي حاجة ليها علاقة بالجلسة أو البيانات الشخصية.
5. **ممنوع `dangerouslySetInnerHTML`.**
6. **ممنوع `window.location.href =`** للتنقّل الداخلي — `router.push` أو `<Link>`.
7. **ممنوع `fetch` مباشر** في طبقة الصفحات — كل النداءات من خلال `@carq/api-client`.
8. **ممنوع استيراد طبقة الموك** (`mockDb`, `MOCK_NOW`, `getDemoExhibition`) جوه صفحة — الـhooks هي المسار الوحيد.
9. **ممنوع `row-reverse` يدوي** — الـHTML `dir="rtl"` والمتصفح بيعكس لوحده. (دي عادة React Native مالهاش لازمة هنا.)
10. **ممنوع `target="_blank"` من غير `rel="noopener noreferrer"`.**
11. **ممنوع دارك ثيم** أو أي أثر ليه (`data-theme`, `darkMode`, `ThemeToggle`).
12. **`nextBid` بييجي من السيرفر** — ممنوع تحسبه في الواجهة (A-3).

> `scripts/static-checks.mjs` بيفرض القواعد ١–١٢ تقريبًا. لو ضفت قاعدة جديدة في
> مرحلة ما، **ضيفها في السكريبت كمان** عشان متترجعش تاني.

### ٣٫٢ قواعد الهوية والتصميم

| العنصر | القيمة |
|---|---|
| الحبر الكحلي | `#131A2E` |
| البرتقالي | `#EE4B1E` |
| الخلفية | `#F2F3F7` |
| الخط | Tajawal (400/500/700/800) |
| نصف القطر | 10 · 14 · 18 · 24 · 30 |

`StrokeMotif` منقول بمساراته المحسوبة من الموبايل — نفس الأشكال الخمسة
(`circle · oval · underline · swoosh · ribbon`) بنفس منحنى الرسم. **متغيّرش
مساراته ولا أطوالها.**

### ٣٫٣ قواعد الرسوم البيانية

الباليتة متحقق منها بسكريبت (نطاق الإضاءة، أرضية التشبّع، فصل عمى الألوان، التباين).
**متغيّرش لون بالحس.**

* **فئوية:** ٧ ألوان بترتيب ثابت، والرمادي = «أخرى» (وعاء مش سلسلة).
* **مقارنة مباشرة** (دونات، شرايح مكدّسة): النواة الرباعية بس.
* **متباعدة:** تيل ← رمادي محايد → برتقالي — لأي بيانات ليها اتجاهين حوالين صفر.
* **ألوان الحالة** محجوزة للشارات والبلاطات، عمرها ما تدخل رسم فئوي، ودايمًا مع أيقونة ونص.
* **ممنوع dual-axis نهائيًا** — مقياسين مختلفين = رسمين.
* كل رسم ليه tooltip و«عرض كجدول» وتصدير CSV — مفروضين في `ChartFrame`.
* سلسلة واحدة ⇒ من غير legend (العنوان بيسمّيها).
* الحالة الفاضية مش رسم فاضي: «مفيش بيانات في الفترة دي».

### ٣٫٤ نطاق المشروع — إيه اللي مش شغلك

الريبو ده **واجهات ويب بس**. مفيش فيه داتابيز ولا business logic سيرفر-سايد.
فيه قواعد هندسية عامة صحيحة تمامًا **بس مكانها ريبو الباك اند مش هنا**:

* تشفير الباسوردات (Argon2id/bcrypt) · SQL Injection · Foreign Keys ·
  Transactions · Indexing · Connection Pooling · `SELECT *` · Query optimization
* فرض الـRBAC · Rate limiting الفعلي · حجم الطلب · WAF/DDoS

**متبنيش أي حاجة من دول هنا، ومتضفش داتابيز ولا ORM ولا API server.**
لو لقيت نقطة منهم مهمة: **اكتبها في `docs/BACKEND-CONTRACT.md` كطلب لفريق
الباك اند** واكمّل. الـroute handlers الوحيدة المسموح بناؤها هي
`app/api/session/*` (المرحلة ٢) — ودي وسيط للكوكي مش باك اند.

**بس الويب عليه نصيبه من نفس القواعد**، ودي شغلك:
إرسال التوكن صح · التعامل مع `401`/`429`/`413` · حدود الرفع قبل ما يتبعت ·
منع الطلبات المكررة · timeouts · idempotency · مايسربش بيانات في اللوجز أو
الـstack traces · ومايفترضش إن الإخفاء أمان.

### ٣٫٥ قواعد الشغل نفسه (مضادة لـ«vibe coding»)

1. **دوّر على السبب الجذري.** ممنوع patch فوق patch عشان الـerror اختفى.
   لو صلّحت عَرَض من غير ما تفهم السبب، اكتب كده صراحة في التقرير.
2. **متغيّرش أجزاء ملهاش علاقة** بالمشكلة اللي بتشتغل عليها.
3. **ممنوع تضيف مكتبة جديدة** من غير ما تسأل. الموجود: Next 15 · React 19 ·
   TanStack Query · Tailwind · Recharts · lucide-react · Vitest · Playwright.
   أي إضافة = سؤال في التقرير بمبررها وحجمها وبديلها.
4. **ممنوع `any` أو `@ts-ignore` أو `eslint-disable`** عشان تسكّت خطأ.
   لو مضطر، علّق فوقها السبب وسجّلها كملاحظة.
5. **قبل ما تكتب دالة/مكوّن جديد، دوّر هل موجود** في `packages/ui` أو
   `packages/api-client`. تكرار المنطق = ملاحظة.
6. **أي تعديل مهم، راجع تأثيره على باقي النظام** — خصوصًا `client.ts` و
   `types.ts` و`packages/ui`، دول بيتشاركوا بين التطبيقين.

### ٣٫٦ قواعد الأمان — مش قابلة للتفاوض


1. **صور البطاقة وأوراق المعارض مفاتيح تخزين، مش روابط.** ممنوع تحطها في
   `<img src>` أو تبني رابط بإيدك. بتتعرض برابط موقّع ٥ دقايق، وكل فتحة بتتسجّل،
   وممنوع كاش أو تنزيل. (F-6 · قانون حماية البيانات المصري ١٥١/٢٠٢٠)
2. **أرقام التليفونات مخفية جزئيًا افتراضيًا**، وبتتكشف بضغطة واعية متسجّلة.
3. **التوكن في الذاكرة** — مش localStorage. الـrefresh في httpOnly cookie.
4. **كل أكشن كتابة بيسيب صف تدقيق.** والأكشنات الخطرة (التعاقد · ترقية الدور ·
   الإيقاف · المزاد المتعثر · شارات الثقة) محتاجة **تأكيد مزدوج + سبب مكتوب**.
5. **مفيش حذف.** كل حاجة soft delete، وسجل التدقيق append-only.

---

## ٤. بروتوكول التنفيذ — اقراه مرتين

### ٤٫١ قاعدة المرحلة الواحدة

> **نفّذ المرحلة المطلوبة بس. اكتب تقريرها. قف. استنى كلمة «كمّل».**

* متبدأش المرحلة اللي بعدها من نفسك، ولا «تحضّر ليها»، ولا تلمس ملفاتها.
* لو لقيت مشكلة تخص مرحلة جاية: **سجّلها في سجل الملاحظات وسيبها** — متصلّحهاش دلوقتي.
* الاستثناء الوحيد: مشكلة **P0** (بتكسر البناء أو تسريب بيانات) — صلّحها فورًا
  واكتبها في التقرير بوضوح تحت عنوان «تدخّل خارج نطاق المرحلة».

### ٤٫٢ قاعدة الدليل

**ممنوع تقول «شغّال» من غير دليل.** كل ادعاء في التقرير لازم يكون وراه واحد من:

* مخرجات أمر (`npm run verify` / `npm run test` / `npm run e2e`) منسوخة حرفيًا.
* اختبار Playwright أو Vitest بيمر (باسمه).
* سكرينشوت في `e2e/screenshots/`.
* اقتباس سطر من الكود بمساره ورقم سطره.

جُمَل زي «تم التأكد» أو «كله تمام» من غير دليل = التقرير مرفوض.

### ٤٫٣ قاعدة الغموض

لو المواصفة ساكتة عن نقطة، أو فيها تعارض، أو محتاج قرار منتج:
**متخترعش. اسأل.** حط السؤال في قسم «أسئلة محتاجة قرار» في آخر التقرير، واكمّل
باقي المرحلة، وسيب النقطة دي متعلّقة بوضوح.

### ٤٫٤ قاعدة Git

* فرع لكل مرحلة: `review/phase-0-recon`, `review/phase-1-conformance` … إلخ.
* كوميتات صغيرة موصوفة: `fix(admin/listings): زرار "إعادة تنشيط" كان مش بيبعت idempotency key (L-13)`.
* **ممنوع** تعمل `git push` أو تعدّل على `main` من غير طلب صريح.
* **ممنوع** تكتب أي سر/توكن/مفتاح في أي ملف متتبّع. `.env.local` بس ومتضيفوش للـgit.

### ٤٫٥ صيغة التقرير — إجبارية

كل مرحلة بتخرج ملف: `reports/PHASE-<رقم>-<اسم>.md` بالشكل ده بالضبط:

```markdown
# المرحلة <رقم> — <الاسم>
التاريخ: <YYYY-MM-DD>   الفرع: <branch>   الكوميتات: <عدد>

## ١. الملخّص في ٥ أسطر
<لو المستخدم قرا ٥ أسطر بس، يعرف إيه اللي حصل وإيه القرار المطلوب منه>

## ٢. الحالة
- [ ] المرحلة خلصت بالكامل
- [ ] خلصت جزئيًا — السبب: ...
- [ ] متوقفة — محتاجة قرار: ...

## ٣. اللي اتفحص فعليًا
جدول: البند | إزاي اتفحص | النتيجة

## ٤. الملاحظات (Findings)
جدول مرتّب بالخطورة:
ID | الخطورة | الملف:السطر | القاعدة (كود المواصفة) | الوصف | الحالة
FND-001 | P0 | apps/admin/... :123 | SN-2 | ... | اتصلّح / مفتوح / مؤجّل لمرحلة N

## ٥. اللي اتصلّح
لكل إصلاح: الملف · إيه اللي كان بيحصل · إيه اللي بقى بيحصل · الدليل

## ٦. اللي لسه مكسور أو ناقص
بصراحة كاملة. البند ده أهم بند في التقرير.

## ٧. الأدلة
مخرجات verify · مخرجات e2e · مسارات السكرينشوتس · أسماء الاختبارات الجديدة

## ٨. الخطر اللي دخل على المشروع
أي حاجة عملتها ممكن تكسر حاجة تانية

## ٩. أسئلة محتاجة قرار
مرقّمة، وكل سؤال معاه الخيارات وتوصيتك

## ١٠. الجاهزية للمرحلة اللي بعدها
جاهز / مش جاهز + السبب
```

### ٤٫٦ سجل الملاحظات المركزي

اعمل `reports/FINDINGS.md` من أول مرحلة وحدّثه في كل مرحلة. ده السجل الوحيد
اللي بيتراكم: كل ملاحظة ليها ID ثابت من أول ما تظهر لغاية ما تتقفل.

### ٤٫٧ الخطورة

| المستوى | التعريف |
|---|---|
| **P0** | تسريب بيانات · تجاوز صلاحيات · فقد فلوس · البناء بيقع · بيانات بتتدمّر |
| **P1** | فيتشر أساسي مش شغّال · مخالفة صريحة لقاعدة في المواصفة · خطأ بيوصل للمستخدم |
| **P2** | سلوك ناقص · حالة حافة مش متعامل معاها · خطأ كونسول · مشكلة a11y |
| **P3** | تحسين · تنظيف · تحسين نص · اتساق بصري |

---

## ٥. المراحل

---

### المرحلة ٠ — الاستطلاع وخط الأساس (قراءة فقط)

**ممنوع في المرحلة دي أي تعديل على أي ملف مصدر.** إنت بتصوّر الوضع الحالي بس.

**المطلوب:**

1. اقرا `docs/ADMIN_DASHBOARD_SPEC.md` و `docs/EXHIBITION_PORTAL_SPEC.md`
   **بالكامل**، و `README.md` و `DEPLOYMENT.md`. اعمل فهرس لكل كود قاعدة
   (`A-1`, `SN-2` …) ومعناه ومكانه في المواصفة → `reports/RULES-INDEX.md`.
2. `npm install` وبعدين شغّل خط الأساس واحفظ المخرجات حرفيًا:
   * `npm run typecheck`
   * `npm run test`
   * `npm run checks`
   * `npm run verify`
   * `npm run e2e` (لو Playwright عايز متصفحات: `npx playwright install chromium`)
3. اعمل **جرد شامل** في `reports/INVENTORY.md`:
   * كل مسار (route) في التطبيقين + الملف بتاعه + هل ليه حماية ولا لأ.
   * كل hook في `api-client` + الـendpoint اللي بينده عليه + مصدره (موك/http).
   * كل مكوّن في `packages/ui` + فين بيتستخدم.
   * كل رسم بياني + كود الـchart (`C-xx`) المقابل له.
   * **كل عنصر تفاعلي** في كل شاشة: زرار، لينك، حقل، فلتر، تاب، dialog، رفع ملف،
     تصدير، ترقيم صفحات، فرز. (ده هيبقى أساس المرحلة ٧ — خده بجد.)
4. حدّد الفجوات الكبيرة اللي إنت شايفها من قراءة الكود بس (من غير إصلاح).
5. اطبع ملخص: كام سطر كود، كام شاشة، كام hook، كام اختبار، كام ملاحظة أولية.

**مخرجات:** `reports/PHASE-0-RECON.md` · `reports/RULES-INDEX.md` ·
`reports/INVENTORY.md` · `reports/FINDINGS.md` (مبدئي)

**قف واستنى.**

---

### المرحلة ١ — مطابقة المواصفات، شاشة بشاشة (قراءة + إصلاحات P0/P1 الصغيرة بس)

**الهدف:** تثبت — أو تنفي — إن كل شاشة مطابقة للمواصفة بالحرف.

**الطريقة:** لكل شاشة في `ADMIN §4.1–4.9` و `PORTAL §4.1–4.7`:

1. افتح قسم المواصفة بتاع الشاشة، واطلع منه **قائمة تحقق مرقّمة** بكل سلوك مطلوب.
2. افتح ملف الشاشة واقراه سطر سطر.
3. شغّل الشاشة فعليًا على المتصفح (Playwright أو يدوي) وشوفها بعينك.
4. لكل بند: مطابق / مخالف / ناقص / مش قابل للتحقق دلوقتي (ليه).

**ركّز بالذات على:**

* `ADMIN §4.2` بيع حالًا — الطابور، SLA، عرض السعر، التحصيل، حالات `SN-1..SN-7`.
* `ADMIN §4.4` المعارض والتعاقد — شروط فتح المزايدة (الـ٣ شروط)، الشارات.
* `ADMIN §4.5` المزادات — المتأخرة عن القفل، المتعثرة، الدخول المدفوع.
* `ADMIN §4.9` سجل التدقيق — append-only، السبب المكتوب.
* `PORTAL §2` التسجيل والترقية وحالات الطلب (§2.3) والشاشات المقابلة.
* `PORTAL §4.3` الرفع بالجملة — التحقق صف صف، الصفوف المكسورة.
* `PORTAL §4.5` غرفة المزايدة الحية — `A-0..A-13` كلها.
* `PORTAL §4.6` رسوم الدخول والفوترة — `ENTRY_UNPAID`.
* `ADMIN §5` كتالوج التشارتس — كل `C-xx` موجود؟ بالنوع الصح؟ بالباليتة الصح؟
* `ADMIN §7` قواعد الألوان والرسم — خصوصًا ممنوع dual-axis وقاعدة الـlegend.
* `ADMIN §8` RTL والصياغة — الأرقام، التواريخ بتوقيت القاهرة، صياغة العربي.
* بيانات الموك المصمّمة للحالات الصعبة: إعلانات draft محبوسة، إعلانات مش
   متسعّرة، طلبات متأخرة عن الـSLA، مزادات متأخرة عن القفل، معارض مش متعاقدة —
   **كل حالة من دول لازم يكون ليها معالجة ظاهرة في الواجهة.**

**الإصلاح المسموح في المرحلة دي:** P0 و P1 اللي إصلاحها أقل من ٢٠ سطر ومش بيلمس
معمارية. أي حاجة أكبر → سجّلها واقترح مرحلة ليها.

**مخرجات:** `reports/PHASE-1-CONFORMANCE.md` + ملف `reports/SPEC-MATRIX.md`
(مصفوفة: الشاشة × البند × الحالة) + تحديث `FINDINGS.md`.

**قف واستنى.**

---

### المرحلة ٢ — الأوثنتيكيشن (بناء فعلي)

**الوضع الحالي اللي إنت داخل عليه** (تحقق منه بنفسك الأول، متصدقنيش):

* صفحة الدخول بتعمل تأخير وهمي وبعدين `tokenStore.set('demo-access-token', 900)` و`router.push('/')`.
* **مفيش `middleware.ts`.** **مفيش أي route handler تحت `src/app/api/`.**
* `(dash)/layout.tsx` و `(portal)/layout.tsx` بيرجّعوا `<Shell>` من غير أي فحص جلسة.
* `client.ts` بينده على `/api/session/refresh` — **الـendpoint ده مش موجود**.
* `tokenStore` مش مستخدم في أي مكان غير صفحتين الدخول.
* الـShell فيه مستخدم ثابت مكتوب بالإيد («مصطفى») و**مفيش زرار خروج**.
* يعني: **أي حد بيفتح `localhost:3100/listings` مباشرة بيدخل من غير دخول.**

**المطلوب تبنيه:**

1. **طبقة جلسة حقيقية** في `packages/api-client`:
   * `useSession()` / `SessionProvider` — بيرجّع `{ user, role, status, isLoading }`.
   * `useMe()` → `GET /v1/me` (موك دلوقتي، http لما الباك يجهز).
   * `logout()` — بيمسح التوكن، بيمسح كاش react-query، بيمسح الكوكي، بيروّح `/login`.
2. **Route handlers** تحت `apps/*/src/app/api/session/`:
   * `POST /api/session` — بياخد نتيجة الـOTP، بيكتب الـrefresh في **httpOnly · Secure ·
     SameSite=Lax** cookie، بيرجّع الـaccess token للذاكرة بس.
   * `POST /api/session/refresh` — بيقرا الكوكي، بيجدّد، بيرجّع `{accessToken, expiresIn}`.
     (ده اللي `client.ts` بينده عليه دلوقتي ومش لاقيه.)
   * `POST /api/session/logout` — بيمسح الكوكي.
   * الـhandlers دول لازم يشتغلوا في وضع الموك كمان (وضع ديمو صريح ومعلّم).
3. **حماية المسارات:**
   * `middleware.ts` لكل تطبيق: أي مسار جوه `(dash)` أو `(portal)` من غير كوكي
     جلسة صالحة → redirect لـ`/login?next=<المسار>`.
   * حماية على مستوى التخطيط كمان (server component check) — مش الـmiddleware لوحده.
   * `/login` وهو داخل بالفعل → redirect للصفحة الرئيسية.
   * `apply/*` في المعارض: مسار عام مقصود — **أكّد ده من `PORTAL §2`** واتعامل معاه صح.
4. **التجديد الصامت:** عند التحميل، الصفحة بتحاول `refresh` مرة واحدة قبل ما
   تقرر إن المستخدم مش داخل. من غير ده، أي `F5` بيطلّع المستخدم بره (التوكن في الذاكرة).
5. **منطق الـ401:** موجود في `client.ts` (تجديد مرة واحدة ثم خروج) — **اختبره فعليًا**
   بموك بيرجّع 401، وتأكد إنه مش بيعمل لوب لا نهائي مع عدة طلبات متوازية
   (لازم يكون فيه refresh واحد مشترك مش واحد لكل طلب — deduplication).
5b. **التجديد الاستباقي — باج مؤكّد:** `tokenStore.isExpiring()` معرّفة في
   `client.ts:40` و**محدش بينادينها في المشروع كله** (اتأكد بـ`grep`).
   يعني النظام بيستنى الـ`401` يقع وبعدين يفكّر في التجديد — وده معناه إن أول
   طلب بعد ١٥ دقيقة بياخد round-trip زيادة، وفي غرفة المزايدة ده وقت مايتحملش.
   المطلوب: جدّد **قبل** الانتهاء (الـaccess عمره ١٥ دقيقة والـrefresh ٣٠ يوم
   وبيتدوّر مع كل استخدام — `I-4`)، مع قفل واحد يمنع تعدد طلبات التجديد
   المتوازية (refresh mutex/single-flight). والـWS كمان محتاج
   `{type:'auth', token: newAccessToken}` على السوكت الشغّال (`R-4`) — اربط
   التجديد بيه (المرحلة ٦).
6b. **Timeout — ناقص خالص:** `http()` مفيهاش `AbortController` ولا أي مهلة
   (اتأكد بـ`grep -rn "AbortController\|signal" packages/api-client/src/`).
   طلب بيعلّق = spinner للأبد من غير رسالة. ضيف مهلة معقولة + إلغاء عند فك
   التركيب، وميعاد أقصر للطلبات التفاعلية من التقارير الثقيلة.
6. **الدخول نفسه:** الـendpoints دي **موجودة وشغّالة في الباك اند بالفعل**
   (`PORTAL §8.1`) — إنت بتوصّل مش بتخترع. اربط الفلو الحقيقي:
   `POST /v1/auth/otp/request` ← `POST /v1/auth/otp/verify` ← `GET /v1/me`، ولو
   `user.role !== 'admin'` في لوحة الأدمن: امسح التوكن واطلع **من غير ما تقول إن فيه داشبورد**.
   في وضع الموك، خلّي ده مسار كامل بموك قابل للاختبار — مش `setTimeout`.
7. **وضع الديمو** يفضل شغّال (أي رقم مصري صحيح + أي كود ٤ أرقام) **بس**:
   * يتفعّل بمتغيّر بيئة صريح، مش افتراضي صامت.
   * يطبع تحذير واضح في الكونسول والواجهة إنه ديمو.
   * **يتقفل تلقائيًا** لما `NEXT_PUBLIC_API_URL` يتحط.
8. **UI:** زرار خروج في الـShell، اسم المستخدم ودوره من الجلسة مش مكتوبين بالإيد،
   حالة تحميل الجلسة، وشاشة «الجلسة انتهت».
9. **اختبارات:** وحدات لمنطق الجلسة، و e2e لـ: دخول ناجح، دخول مرفوض، وصول
   مباشر لمسار محمي بدون جلسة، `F5` بيحافظ على الجلسة، خروج، انتهاء الجلسة.

**قيود:** التوكن في الذاكرة بس. ممنوع أي storage. الكوكي httpOnly. `credentials: 'include'` موجود بالفعل — تأكد إنه متسق مع إعدادات CORS المتوقعة ووثّق المتطلب للباك.

**مخرجات:** `reports/PHASE-2-AUTH.md` + `docs/AUTH.md` (وصف الفلو والعقود
المطلوبة من الباك اند).

**قف واستنى.**

---

### المرحلة ٣ — الصلاحيات والأدوار (RBAC)

**المرجع:** `ADMIN §2` (الصلاحيات) و `PORTAL §5` (الصلاحيات داخل البوابة) و
`PORTAL §6` (الفريق — مرحلة ٢، وثّقه بس متبنيهوش من غير طلب).

**المطلوب:**

1. **نموذج صلاحيات مركزي واحد** في `api-client` — مش شروط `if` مبعترة في الصفحات:
   * `can(user, 'exhibition.contract.set')` أو ما يعادله.
   * مصفوفة الصلاحيات مكتوبة في ملف واحد ومتوثّقة.
2. **جانب الأدمن:** `role === 'admin'` هو شرط الدخول. أي دور تاني = مفيش داشبورد أصلًا.
3. **جانب المعارض:** طبّق الأدوار زي ما `PORTAL §5` بيحدد بالحرف. أي دور في
   المواصفة ومش متطبّق = ملاحظة P1.
4. **بوابات الحالة مش بس الدور:**
   * معرض **مش متعاقد** → مايشوفش/مايدخلش المزادات (الشروط الـ٣ في `ADMIN §4.4`).
   * طلب ترقية `pending/rejected` → الشاشات المقابلة في `PORTAL §2.3` بالظبط.
   * رسوم دخول مش مدفوعة → `ENTRY_UNPAID` ومنع المزايدة.
5. **قاعدة الإخفاء مش كفاية:** أي زرار مخفي لازم يكون الأكشن نفسه محمي كمان
   (حارس على مستوى الـhook/الـmutation)، وموثّق إن الباك لازم يفرضه سيرفر-سايد.
   **اكتب صراحة في التقرير:** الواجهة مش حدود أمان — الباك هو الحد.
6. **الأكشنات الخطرة:** التعاقد · ترقية الدور · الإيقاف · المزاد المتعثر · شارات
   الثقة → تأكيد مزدوج + **سبب مكتوب إجباري** + صف تدقيق. تحقق إنها كلها كده.
7. **اختبارات:** لكل دور × كل مسار: مسموح/ممنوع. جدول كامل في التقرير.

**مخرجات:** `reports/PHASE-3-RBAC.md` + `docs/PERMISSIONS.md` (مصفوفة الدور × الأكشن).

**قف واستنى.**

---

### المرحلة ٤ — الأمان والخصوصية

**المرجع:** `ADMIN §10` و `PORTAL §10` و `security-headers.mjs`.

**المطلوب:**

1. **صور البطاقة والأوراق (F-6):** اتأكد إن مفيش مكان واحد بيحط مفتاح تخزين في
   `<img src>` أو بيبني رابط بإيده. المسار الوحيد: `useSignedIdImage` → رابط
   موقّع ٥ دقايق → عرض → تسجيل الفتحة → منع الكاش والتنزيل.
   تحقق من: `Cache-Control`, `download` attribute, right-click, drag, وطباعة الصفحة.
2. **إخفاء التليفونات:** افتراضيًا مخفي جزئيًا في **كل** جدول وكارت وتفصيل
   وتصدير CSV. الكشف بضغطة واعية + تسجيل. **تحقق من التصدير بالذات** — CSV
   بيسرّب أرقام كاملة بسهولة.
3. **CSP:** `script-src` فيه `'unsafe-inline'` دلوقتي (بسبب سكريبتات إقلاع Next).
   قيّم ترقية نمط الـnonce عن طريق الـmiddleware اللي بنيته في المرحلة ٢ —
   دلوقتي بقى عندك middleware أصلًا. لو الترقية ممكنة من غير كسر، اعملها؛ لو لأ،
   وثّق ليه.
4. **باقي الهيدرز:** تحقق من كل هيدر في `security-headers.mjs` إنه بيوصل فعلًا
   في الرد (اختبار Playwright بيقرا الهيدرز — فيه واحد موجود، وسّعه).
   ضيف `Strict-Transport-Security` للإنتاج لو مش موجود.
5. **XSS:** كل مكان بيعرض نص جاي من المستخدم (اسم معرض، سبب رفض، رسالة محادثة،
   اسم موديل من رفع بالجملة) — اختبره بحمولة XSS فعلية في الموك.
6. **CSRF:** الـroute handlers الجديدة + أي POST — تحقق من `SameSite` وأصل الطلب.
7. **التدقيق:** كل mutation في `api-client` — هل بيولّد صف تدقيق؟ اعمل جدول:
   الـmutation × صف التدقيق × السبب المكتوب × التأكيد المزدوج.
8. **مفيش حذف:** ابحث عن أي `delete` فعلي. `useDeleteListing` موجود — تأكد إنه
   soft delete بالفعل مش hard.
9. **تسريب في اللوجز:** ابحث عن أي `console.log` بيطبع بيانات شخصية. شيلها.
10. **التبعيات:** `npm audit` — سجّل النتيجة. متعملش `audit fix --force` من غير طلب.
11. **البناء الإنتاجي:** تأكد إن مفيش source maps بتتنشر بمحتوى حساس، ومفيش
    متغيّرات بيئة سرّية طالعة تحت `NEXT_PUBLIC_`.

**مخرجات:** `reports/PHASE-4-SECURITY.md` + `docs/SECURITY-REVIEW.md`
(قائمة الضوابط × الحالة × الدليل) + توسعة `e2e/security.spec.ts`.

**قف واستنى.**

---

### المرحلة ٥ — جاهزية الربط بالباك اند

**الادعاء المكتوب في README:** «التبديل للباك اند مش إعادة كتابة — `USE_MOCK`
بيتحوّل لـ`false` لوحده أول ما يلاقي عنوان API، ولا شاشة واحدة بتتغير.»
**شغلتك تثبت الادعاء ده أو تكسره.**

**المطلوب:**

1. **اختبار التبديل فعليًا:** قوم بسيرفر موك بسيط (MSW أو سيرفر Node صغير) بيرد
   بالعقود اللي في `types.ts`، حط `NEXT_PUBLIC_API_URL` عليه، وشغّل التطبيقين.
   **كل شاشة تشتغل ولا لأ؟** أي شاشة بتقع = P0.
2. **جرد العقود:** لكل hook → الـendpoint، الـmethod، الـquery params، شكل الرد،
   شكل الخطأ. قارن بـ`ADMIN §6.1/6.2/6.3` و `PORTAL §8.1/8.2/8.3`.
   أي فرق بين اللي الكود بيتوقعه واللي المواصفة بتقوله = ملاحظة.
3. **الـendpoints الناقصة:** اعمل `docs/BACKEND-CONTRACT.md` — مستند واحد
   يتسلّم لفريق الباك اند، فيه لكل endpoint ناقص:
   المسار · الميثود · الـparams · شكل الطلب · شكل الرد · أكواد الأخطاء · مثال
   حقيقي من بيانات الموك · الشاشة اللي واقفة عليه.
   على الأقل اللي مذكور في README:
   * `GET /v1/admin/stats/*` — `overview` · `timeseries` · `breakdown` · `funnel`
     كلهم بياخدوا فلاتر `§4.1` (`days` أو `from`+`to` · `governorate` · `make`) كـquery params.
     * `timeseries?metric=` : `listings_per_day` · `new_users` ·
       `sell_now_requests` (سلسلة لكل حالة) · `financing_by_type` (مرابحة/عادي) ·
       `auctions_created` (سلسلة لكل حالة).
     * `breakdown?dimension=` : `listing_status` · `governorate` · `make` ·
       `price_band` · `auction_status` · `bids_by_exhibition` ·
       `financing_monthly` (باكتات القسط من `quote_snapshot`) ·
       `sellnow_value` (قيمة العروض المتصدَّرة بالحالة).
   * `GET /v1/admin/stats/admin-activity` → `{cells: [{day, hour, count}]}`
     **بتوقيت القاهرة** — الهيت ماب `C-52`.
   * `POST /v1/admin/exhibitions` — مفيش endpoint بينشئ معرض أصلًا.
   * `PATCH /v1/admin/users/{id}/role` — الشرط ١ من ٣ للمزايدة.
   * `GET .../id-image` — الرابط الموقّع لصور البطاقة.
   * `POST /v1/exhibitions/applications` + كل طلبات الترقية.
   * `POST /v1/listings/{id}/reactivate` — «متباعة» → «نشطة» (L-13، بـidempotency key).
   * المحادثات: `GET /v1/chats/{id}/messages` →
     `{items: [{id, threadId, from: 'exhibition'|'buyer', body, at}]}` ·
     `POST /v1/chats/{id}/messages` بجسم `{body}` + idempotency key ·
     `POST /v1/chats/{id}/read`.
   * تضمين ملخص الإعلان في `GET /v1/auctions` و `GET /admin/sell-now/requests`
     (غير كده الجداول بتعمل N+1) — **تحقق من الـN+1 ده فعليًا في الموك وسجّله**.
   * `GET /v1/me/exhibition/stats` محتاج يرجّع `sold_at` لكل بيعة عشان
     sparkline الـ`D-06` (متوسط أيام حتى البيع).
4. **أكواد الأخطاء:** الواجهة بتفهم الأسماء البديلة `ENTRY_UNPAID` · `SELF_BID` ·
   `AUCTION_CLOSED` جنب الأساسية. راجع `errors.ts` و `types.ts`: هل كل كود خطأ في
   المواصفة ليه رسالة عربية مفهومة؟ أي كود مش متغطّى بيوصل للمستخدم كـ«حصل خطأ
   في الاتصال بالسيرفر» — ده مش مقبول.
5. **Idempotency — فيه باج في النمط نفسه:** `idempotencyKey()` بترجّع
   `${prefix}-${Date.now()}-${random}` يعني **مفتاح جديد مع كل نداء**. وهي
   بتتنادى **جوه** الـ`mutationFn` في معظم الـhooks (`dealers/hooks.ts:123, 297,
   315, 380, 399, 419, 466` و`admin/hooks.ts:194`). النتيجة: أي إعادة محاولة أو
   دوسة تانية سريعة = مفتاح مختلف = **العملية بتتنفّذ مرتين**، وده بالظبط اللي
   الـidempotency موجودة تمنعه. (لاحظ إن الرفع بالجملة عامل الصح:
   `bulk-${uploadId}-${index}` مفتاح ثابت — استخدمه كنموذج.)
   المطلوب: المفتاح يتولّد **مرة واحدة لكل عملية منطقية** ويفضل ثابت عبر كل
   المحاولات. وبعدين اعمل جرد كامل: كل mutation × بتبعت مفتاح ولا لأ × المفتاح
   ثابت ولا لأ. المزايدة والدفع والتحصيل أخطر التلاتة.
5b. **التعامل مع أكواد HTTP التشغيلية:**
   * `429 / RATE_LIMITED` — متعامل معاه **في غرفة المزايدة بس**
     (`auctions/[id]/page.tsx:281` وبيعطّل ثانيتين من غير إعادة محاولة تلقائية —
     ده صح). **مش متعامل معاه في أي مكان تاني**، والـOTP في صفحة الدخول بيعتمد
     على عدّاد ٣٠ ثانية في الواجهة بس. عمّم المعالجة، ووثّق إن الفرض الحقيقي
     لازم يكون في الباك (الدخول · OTP · المزايدة).
   * `413` — الرفع. حدود الحجم متطبّقة في `apply/page.tsx:127,136` بس،
     ومش موجودة في `inventory/new` ولا `inventory/[id]` ولا `profile` ولا
     الـCSV في `bulk`. وحّد الحدود (حجم + نوع + عدد) **قبل** الإرسال، ومعاها
     رسالة عربية واضحة، ومعالجة للرد لو السيرفر رفض برضو.
   * `5xx` وانقطاع الشبكة — رسالة مفهومة مش «حصل خطأ في الاتصال بالسيرفر» الجافة.
5c. **مايسربش تفاصيل داخلية:** تأكد إن مفيش stack trace ولا رسالة داتابيز ولا
   جسم رد خام بيوصل للمستخدم في `error.tsx` أو في الـtoasts. والفرق بين
   dev overlay والإنتاج متحقق منه فعليًا على بناء إنتاجي.
5d. **`X-Request-Id` / Correlation ID:** ولّد معرّف لكل طلب من الواجهة وابعته
   في الهيدر، واعرضه في رسالة الخطأ للمستخدم («رقم المرجع: …»). من غيره، لما
   معرض يشتكي من مزايدة فشلت مش هتعرف تلاقي الطلب في لوج الباك. وثّق المتطلب
   للباك اند إنه يسجّله ويرجّعه.
5e. **الطلبات المكررة:** TanStack Query بيعمل dedup للـqueries بنفس الـkey —
   **تحقق إن ده شغّال فعلًا** مش متكسّر بـkeys متغيّرة كل رندر (كائن فلاتر جديد
   كل مرة = key جديد = طلب جديد). راقب تاب الشبكة وشوف أي مورد بيتطلب أكتر من
   مرة في نفس اللحظة من مكوّنات مختلفة.
6. **الترقيم والفلاتر:** كل جدول — هل الترقيم سيرفر-سايد فعلًا (`Page<T>`)
   ولا بيجيب كل حاجة ويقسّم في الواجهة؟ لو التاني، ده مش هيستحمل بيانات إنتاج.
7. **مسار توليد الأنواع:** جرّب `npx openapi-typescript` على schema تجريبي
   وتأكد إن `types.ts` قابل للاستبدال فعلًا من غير ما يكسر الصفحات
   (يعني: الصفحات بتستورد أنواع من `@carq/api-client` مش بتعرّفها لنفسها).
8. **البيئة:** وثّق كل متغيّر بيئة مطلوب في `.env.example` (من غير قيم حقيقية):
   `NEXT_PUBLIC_API_URL` · `NEXT_PUBLIC_STORAGE_URL` · متغيّر وضع الديمو · أي حاجة جديدة.

**مخرجات:** `reports/PHASE-5-BACKEND-READINESS.md` + `docs/BACKEND-CONTRACT.md`
+ `.env.example` + اختبارات عقود (contract tests) بتشتغل على سيرفر الموك.

**قف واستنى.**

---

### المرحلة ٦ — الريل تايم والاتساق عبر الأسطح

**الوضع الحالي في كود الويب:** polling بس عن طريق `refetchInterval`:
`POLL = { queue: 20s, health: 30s, stats: 5m }` في الأدمن، و في المعارض قيم
`10s/15s/60s/300s`. المحادثات بتعمل بولينج ١٠ ثواني والمحادثة مفتوحة.
غرفة المزايدة `(portal)/auctions/[id]` بتعمل polling، ومكتوب في تعليقاتها إنه
«بديل مؤقت للـWebSocket في المرحلة ١».

**الوضع الحقيقي في النظام — مهم:** الـWebSocket **موجود وشغّال في الباك اند**
على `{API_URL}/v1/ws` بمواضيع `chat:{thread_id}` · `auction:{auction_id}` ·
`user:{user_id}`. البروتوكول موصوف بالحرف في `PORTAL §4.5`. يعني غرفة المزايدة
تقدر تبقى لايف **من غير أي شغل باك اند جديد** (`ADMIN §9` بيقول كده صراحة).

**وفي نفس الوقت: الـpolling في لوحة الأدمن قرار مقصود مش نقص** — مفيش موضوع
`admin:*` في الباك، والمواصفة (`ADMIN §9`) بتحدد الـpolling كـ«المرحلة ١»
بالقيم الموجودة بالظبط. **متحوّلش لوحة الأدمن لـWS.**

**المطلوب:**

1. **اقرا `PORTAL §4.5` كامل قبل أي سطر كود**، ومعاه `ADMIN §9`، ومعاهم
   المرجع التنفيذي في ريبو الموبايل (`src/api/auctions.ts::subscribeToAuction`).
   البروتوكول مكتوب — **متصممش واحد من عندك**:
   * الإطارات: `{ type, topic, event_id, data }`
   * `{type:'auth', token}` في أول ١٠ ثواني وإلا السوكت بيتقفل → `auth.ok`
   * `{type:'subscribe', topic:'auction:{id}', since: lastEventId}` → `subscribed`
     ثم الأحداث الفايتة (`R-3` — استرجاع اللي فاتك)
   * الأحداث: `bid.placed` · `auction.extended` · `auction.ended` بحمولاتها المحددة
   * `{type:'ping'}` → `pong`
   * تجديد التوكن على سوكت شغّال: `{type:'auth', token: newAccessToken}` (`R-4`)
2. **القواعد الإلزامية للغرفة — دي أهم ٣ بنود في المرحلة:**
   * **REST الأول، WS بعدين.** `GET /auctions/{id}` + `/bids` عند كل فتح **قبل**
     الاشتراك. الـWS بيكمّل، مابيبدأش.
   * **ممنوع إعادة اتصال تلقائي جوه الغرفة في النسخة الأولى** — دي عقيدة متعمّدة
     منقولة من الموبايل. **بدالها بانر إلزامي** «الاتصال اتقطع — تحديث» بيسحب
     REST تاني. مزايد شايف رقم بايت هيخسر فلوس.
     (لو لقيت نفسك بتكتب exponential backoff داخل الغرفة، إنت بتكسر القاعدة دي.)
   * **`auction.extended` بيغيّر `endsAt`.** اقرا الـ`endsAt` الجديد مع كل حدث،
     واعرض التمديد **صراحة**: «المزاد اتمدّ ٦٠ ثانية». التمديد ٦٠ ثانية بحد أقصى
     ٢٠ مرة (`A-5`). عداد بيقفز من غير تفسير بيبان باج.
3. **طبقة تجريد** في `api-client`: `useRealtime(topic, handler)` — واجهة واحدة
   ورا الـWS للمواضيع الموجودة، وورا الـpolling للي لسه مالوش موضوع.
   نفس فلسفة `USE_MOCK`: نقطة تبديل واحدة، والشاشات مابتعرفش الفرق.
   لازم تشتغل في وضع الموك كمان (موك سوكت) عشان الاختبارات تمشي بدون باك.
4. **مصفوفة الإبطال (invalidation matrix):** لكل حدث، أنهي `queryKey` بيتبطّل.
   ملف واحد. دلوقتي الـinvalidation مبعتر في الـmutations — راجعه كله.
5. **نموذج الأحداث الكامل** في `docs/REALTIME.md`: كل حدث لازم ينتشر بين
   الأسطح الثلاثة (أدمن · بوابة المعارض · موبايل)، ومعاه **الفرق بين اللي
   متغطّى بموضوع WS موجود واللي لسه محتاج موضوع جديد من الباك**:
   * متغطّى دلوقتي: مزايدة · تمديد · قفل مزاد · رسالة محادثة · أحداث المستخدم.
   * **محتاج موضوع `admin` جديد** (`ADMIN §9` المرحلة ٢): `sell_now.requested` ·
     `auction.settled` · `scan.failed`. اكتب العقد المطلوب للباك اند وسيبه polling لحد ما يجهز.
   * مش واضح ليه موضوع: تعاقد معرض · تغيير حالة إعلان · دفع رسوم دخول ·
     تغيير دور. حدّدها كطلب صريح للباك، ومتخترعش ليها حل مؤقت معقّد.
6. **سلوك الـpolling السليم** (للي هيفضل polling):
   * بيقف لما التاب مش ظاهر (`visibilitychange`) — polling كل ١٠ ثواني في تاب
     مقفول على ٢٠ معرض = ضغط مجاني على الباك.
   * backoff عند الخطأ — **بره غرفة المزايدة بس** (شوف بند ٢).
   * مفيش تسريب `setInterval` عند فك التركيب (اختبرها).
7. **الاتساق عبر الأسطح — اختبر فعليًا بمتصفحين مفتوحين:**
   * معرض بيزايد في 3200 → الأدمن في 3100 بيشوف المزايدة خلال دورة الـpolling.
   * الأدمن بيتعاقد مع معرض → البوابة بتفتح المزادات للمعرض ده.
   * الأدمن بيغيّر حالة إعلان → البوابة بتعكسها.
   * رسالة من المشتري → عداد غير المقروء في البوابة.
   **أي حالة مش بتتزامن = ملاحظة، حتى لو الـpolling «المفروض» يجيبها.**
8. **التحديث المتفائل (optimistic):** المزايدة بالذات — لو الواجهة بتعرض المزايدة
   قبل تأكيد السيرفر، لازم يكون فيه rollback واضح عند الفشل (`SELF_BID`,
   `AUCTION_CLOSED`, `ENTRY_UNPAID`). اتأكد إن `nextBid` بييجي من السيرفر زي ما
   هو (A-3) و`bidStep` بيتقرا من `auctions.bid_step` — **مش hard-coded**.
9. **تعارض السباق:** مزايدتين في نفس اللحظة · قفل المزاد أثناء ما المستخدم بيكتب
   مزايدة · حدث WS وصل قبل ما رد الـREST يخلص · `event_id` مكرر أو ناقص.
   حدّد السلوك المطلوب لكل حالة واختبره.

**متبنيش سيرفر WS.** الموجود منه شغّال — إنت بتبني العميل والتجريد بس.

**مخرجات:** `reports/PHASE-6-REALTIME.md` + `docs/REALTIME.md` + اختبارات
تزامن عبر سياقين في Playwright.

**قف واستنى.**

---

### المرحلة ٧ — اختبار كل زرار (الاختبار الشامل)

دي المرحلة اللي المستخدم طالبها بالحرف: **«تيست كل حاجه، كل زرار، كل فيتشر،
والكونسول والايرورز»**. خد `reports/INVENTORY.md` من المرحلة ٠ وامشي عليه بند بند.

**لكل عنصر تفاعلي في المشروع، اختبر:**

1. **إنه موجود** وظاهر في المكان الصح.
2. **إنه بيعمل حاجة** — الضغط بيغيّر حالة/بيبعت طلب/بيفتح dialog. زرار ميت = P1.
3. **حالاته:** طبيعي · hover · focus · disabled · loading · بعد النجاح · بعد الفشل.
4. **الضغط المزدوج/السريع** — مش بيبعت الطلب مرتين.
5. **الكيبورد:** Tab بيوصله، Enter/Space بيفعّله، Escape بيقفل الـdialog، ومفيش
   focus trap مكسور.

**ولكل شاشة، اختبر الحالات الأربعة:**
تحميل · فاضية · خطأ · بيانات كاملة. (بيانات الموك مصمّمة مخصوص للحالات الصعبة —
استخدمها: draft محبوسة، مش متسعّرة، متأخرة عن SLA، مزادات متأخرة عن القفل،
معارض مش متعاقدة.)

**ولكل فورم:**
* تحقق كل حقل: فاضي · أقل من الحد · أكتر من الحد · رموز · مسافات · عربي · إنجليزي · أرقام عربية.
* رقم الموبايل: `^01[0125]\d{8}$` — جرّب اللي بره النمط.
* الحفظ وهو dirty، الخروج وهو dirty (تحذير؟)، الحفظ مرتين.
* رفع الملفات: نوع غلط · حجم كبير · ملف فاضي · صفر ملفات · ملفات كتير · CSV مكسور
  في الرفع بالجملة (`PORTAL §4.3`).

**واختبارات الحدود (IDOR وعزل المستأجرين) — دي مش موجودة حاليًا وناقصة:**
* غيّر الـ`[id]` في الـURL يدويًا لمعرّف بتاع معرض تاني / إعلان مش بتاعك /
  طلب بيع حالًا مش في نطاقك. **المفروض ترجع رسالة رفض واضحة، مش بيانات ولا شاشة بيضا.**
* نفس الحاجة على كل مسار `[id]` في التطبيقين — اعمل جدول كامل.
* مستخدم بوابة بيحاول يفتح مسار أدمن (والعكس).
* الأرقام المتسلسلة: لو الـids متوقّعة، سجّلها كملاحظة للباك.
> الفرض الحقيقي في الباك (`require_role` و`_authorize_bidder`)، بس **الاختبار
> بيتعمل من هنا** — وأي حالة بترجع بيانات = P0 تتبلّغ لفريق الباك فورًا.

**ولكل جدول:**
* ترقيم الصفحات: أول · آخر · صفحة مش موجودة في الـURL · تغيير الفلتر وإنت في صفحة ٥.
* الفرز على كل عمود (تصاعدي/تنازلي/إلغاء).
* البحث: نتيجة واحدة · صفر نتائج · محارف خاصة · مسافات زيادة · عربي بتشكيل.
* تصدير CSV: بيتفتح صح · الأرقام مش بتتحوّل لتواريخ · العربي مش بيتكسر
  (BOM) · **التليفونات مخفية**.

**ولكل رسم بياني:**
* tooltip · «عرض كجدول» · تصدير CSV · الحالة الفاضية بنصها الصح · legend
  (سلسلة واحدة = من غير legend) · الباليتة الصح · مفيش dual-axis.

**ومراقبة مستمرة طول الجولة:**
* **الكونسول:** صفر أخطاء وصفر تحذيرات React (keys · hydration · controlled/uncontrolled
  · تحذيرات act). الـhelper `watchConsole` موجود بالفعل ومعاه فلتر ضوضاء — وسّعه ولا تضعّفه.
* **الشبكة:** صفر طلبات فاشلة، صفر 404، صفر طلبات مكرّرة لنفس المورد في نفس اللحظة.
* **مفيش شاشة خطأ من Next** (dev overlay أو صفحة الإنتاج).
* **الروابط المكسورة:** كل `<Link href>` بيودّي لمسار موجود فعلًا. و`not-found.tsx`
  شغّال في التطبيقين.

**التوسعة المطلوبة:** حاليًا فيه ٢٧ اختبار Playwright (11 admin · 8 dealers ·
7 security · 1 visual). التغطية المطلوبة أكبر من كده بكتير. وسّع `e2e/` لحد
ما كل بند في الجرد يكون متغطّى، ونظّم الاختبارات بملف لكل شاشة بدل الملفات الكبيرة.

**سكرينشوتس:** ديسكتوب (1440×900) وموبايل (Pixel 7) لكل شاشة وكل حالة رئيسية
في `e2e/screenshots/`. **راجعها بعينك** واكتب في التقرير أي حاجة شكلها غلط —
تداخل نصوص، قص، RTL مقلوب، رسم مكسور.

**مخرجات:** `reports/PHASE-7-QA.md` + `reports/BUTTON-MATRIX.md` (كل عنصر ×
النتيجة) + اختبارات جديدة في `e2e/` + مجلد سكرينشوتس مراجَع.

**قف واستنى.**

---

### المرحلة ٨ — الجودة: الأداء وإمكانية الوصول والمتانة

1. **إمكانية الوصول (a11y):**
   * كل زرار أيقونة له `aria-label` عربي مفهوم.
   * كل حقل له `<label>` مربوط.
   * الـdialogs: `role="dialog"` · `aria-modal` · focus trap · العودة للزرار بعد الإقفال.
   * تباين الألوان على النصوص الصغيرة (الباليتة متحقق منها للرسوم — النصوص لازم تتفحص كمان).
   * ترتيب الـfocus منطقي في RTL.
   * الجداول: `<th scope>` و`caption` أو بديل.
2. **الأداء:**
   * حجم الحزمة لكل مسار (`next build` بيطبعه) — حدّد أي مسار ضخم وليه.
   * `recharts` و`lucide-react` تحت `optimizePackageImports` بالفعل — تحقق إنه نافع.
   * إعادة الرندر: أي شاشة بترندر أكتر من اللازم مع الـpolling.
   * جداول كبيرة: جرّب ٥٠٠+ صف في الموك وشوف بتتصرف إزاي.
   * تسريب الذاكرة: سيب غرفة المزايدة مفتوحة ١٠ دقايق وراقب.
3. **المتانة:**
   * `error.tsx` و`loading.tsx` لكل route group — موجودين؟ (شوف `not-found.tsx` الموجود).
   * Error boundary حوالين الرسوم البيانية — رسم بيقع مايوقّعش الصفحة.
   * سلوك بدون شبكة / شبكة بطيئة (throttle في Playwright).
   * `Suspense` و streaming — مستخدمين صح ولا كل حاجة client-side؟
4. **RTL والصياغة:** راجع `ADMIN §8` كله. الأرقام (`tnum`)، التواريخ بتوقيت
   القاهرة، صياغة عربية مصرية متسقة، مفيش نص إنجليزي متسرّب في الواجهة.
4b. **الفلوس والوقت — مصدر باجات صامتة:**
   * **ممنوع حساب فلوس بـfloat.** كل الأسعار والمزايدات والرسوم أعداد صحيحة
     (قروش/جنيه) — دوّر على أي `*` أو `/` أو `toFixed` على مبلغ وراجعه.
     تنسيق العملة في مكان واحد (`packages/ui/src/lib/format.ts`) مش مكرر.
   * **مصر بتطبّق التوقيت الصيفي** (رجع من ٢٠٢٣). الهيت ماب `C-52` بيتجمّع
     بـ`{day, hour}` **بتوقيت القاهرة** — يوم التحويل بيبقى ٢٣ أو ٢٥ ساعة.
     اختبر التجميع حوالين تواريخ التحويل، ومعاه عدّادات المزاد وحساب الـSLA
     وفلاتر `from`/`to`. Playwright متظبط على `Africa/Cairo` بالفعل — استغله،
     وضيف اختبار بتاريخ تحويل صريح.
   * أي مقارنة وقت لازم تستخدم `nowMs()` من `api-client/time.ts` مش `Date.now()`
     المباشر — عشان الاختبارات تقدر تثبّت الوقت.
5. **الرصد والملاحظة (observability) — مفيش منها حاجة دلوقتي:**
   * مفيش أي أداة رصد أخطاء في الإنتاج. يعني لو شاشة وقعت عند معرض، محدش هيعرف.
     اقترح حل (Sentry أو ما يعادله) **واسأل قبل ما تضيف أي مكتبة** — وتأكد إن
     أي حل مايبعتش أرقام تليفونات ولا صور بطاقات ولا توكنز في تقرير الخطأ.
   * `console.log` بتاع البيانات الشخصية: اتشال في المرحلة ٤ — أكّد إنه فضل متشال.
   * **التدهور اللطيف:** خدمة غير أساسية واقعة مايوقّعش الصفحة. جرّب:
     `/health` بترجع خطأ → اللوحة لازم تفضل شغّالة والبلاطة بس تقول إن فيه مشكلة.
     رسم واحد بيقع → باقي الصفحة تفضل.
5. **نظافة الكود:** كود ميت · استيرادات مش مستخدمة · `any` في TS · `@ts-ignore` ·
   `eslint-disable` — كل واحدة تتبرر أو تتشال.

**مخرجات:** `reports/PHASE-8-QUALITY.md`

**قف واستنى.**

---

### المرحلة ٩ — التقرير النهائي وبوابة الإطلاق

1. **جولة كاملة نظيفة:** `npm run verify && npm run e2e` من فرع نضيف، المخرجات كاملة.
2. **`reports/FINAL-REPORT.md`:**
   * حالة المشروع في صفحة واحدة — يتبعت لصاحب المنتج زي ما هو.
   * كل الملاحظات من `FINDINGS.md`: مقفولة / مفتوحة / مؤجّلة، بالخطورة.
   * **إيه اللي لسه واقف على الباك اند** — قائمة مرتّبة بالأولوية.
   * **إيه اللي لسه واقف على تطبيق الموبايل** — `PORTAL §9` بيحدد ٣ حاجات بالظبط:
     (١) زرار «ادفع رسوم الدخول» في `src/app/auctions/[id].tsx` — الدالة
     `payAuctionEntry()` **مكتوبة بالفعل في `src/api/auctions.ts` ومحدش بينادينها**؛
     (٢) شاشة أخطاء المزايدة للأكواد الستة في جدول `§4.5` (كلها دلوقتي بتقع في
     نفس الـcatch العام)؛ (٣) صف «قدّم كمعرض» في `profile.tsx` بيفتح `/apply`.
     **متعدّلش على ريبو الموبايل** — وثّقهم بس كطلب مسلّم لفريق الموبايل.
   * قائمة ما قبل النشر (راجع `DEPLOYMENT.md`): متغيّرات البيئة · الدومينات
     (`admin.carq.eg` · `dealers.carq.eg`) · فصل المشروعين على Vercel · الهيدرز ·
     الـrobots.
   * تقييم صريح: **المشروع جاهز للإطلاق ولا لأ، وليه.** بصراحة كاملة.
3. **`docs/` المسلّمة للباك اند:** `BACKEND-CONTRACT.md` · `REALTIME.md` ·
   `AUTH.md` · `PERMISSIONS.md` · `SECURITY-REVIEW.md` — مراجَعين ومتسقين.
4. **Definition of Done:** راجع `ADMIN §11` و `PORTAL §11` بند بند وقول أنهي بند
   متحقق وأنهي لأ.

---

## ٦. ابدأ من هنا

1. اقرا الملف ده كله. لو فيه حاجة مش واضحة، اسأل قبل ما تبدأ.
2. اعمل `reports/` و`git checkout -b review/phase-0-recon`.
3. نفّذ **المرحلة ٠ بس**.
4. اكتب `reports/PHASE-0-RECON.md` بالصيغة الإجبارية.
5. **قف.** اطبع في المحادثة ملخص ٥ أسطر + السؤال: «أكمّل للمرحلة ١؟»

متبدأش المرحلة ١ قبل ما تسمع «كمّل».
